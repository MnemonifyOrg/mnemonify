# Claude Code task: Apply Mnemonify branding

Use the included `Mnemonify-Brand-Kit` folder as the sole source of truth for Mnemonify visual branding.

Requirements:
- Read `BRAND-GUIDE.md` before modifying the app.
- Use SVG logos as canonical sources.
- Preserve the approved 1:1 M-mark geometry and intrinsic aspect ratio.
- Import `tokens/mnemonify-tokens.css` globally and replace hard-coded brand colors with `--mn-*` variables where appropriate.
- Use DM Sans for display text and Inter for UI text, with the specified fallbacks.
- Update the app logo, splash screen, favicon, and app icons with the supplied files.
- Keep the tagline exactly: “Learning creation for everyone.”
- Keep the values exactly: Open, Simple, Flexible, Community.
- Do not stretch, skew, recolor, or redraw the logo.
- After implementation, list every modified file and summarize each branding change.
