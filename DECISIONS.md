# Decisions Log
Running record of architectural choices made and why.
Format: Date | Decision | Reason
---

2026-07-11 | Defined concrete `content` JSON shapes for the heading, image, list, tabs, and knowledge-check block types (heading: `{text, level}`; image: `{asset_id}`; list: `{style, items[]}`; tabs: `{items: [{label, body_blocks}]}`; knowledge-check: `{question, options: [{id, text, correct}]}`). ARCHITECTURE.md Section 3.3 only shows worked examples for `text` and `accordion`. | Phase 1 required a hand-written sample course and a schema that validates it, which meant these shapes had to be decided somewhere. Documented here so the schema, player, and future editor/trigger-builder stay consistent with the same content model.

2026-07-11 | Phase 1 trigger engine (`packages/player/src/engine/triggerEngine.js`) fully implements only `SET_VAR` and `ADJUST_VAR`. The other actions in the documented vocabulary (`SHOW_BLOCK`, `HIDE_BLOCK`, `ENABLE_BLOCK`, `DISABLE_BLOCK`, `JUMP_TO_PAGE`, `OPEN_LIGHTBOX`, `SET_STATE`, `SCORM_COMPLETE`, `SCORM_SET_SCORE`) are valid per the schema but log a "not implemented" warning if fired. | Matches REQUIREMENTS.md Section 10 Phase 1 scope: the sample course only exercises an `onOpen` → `SET_VAR` trigger, and the full trigger builder UI/behavior (block visibility, page navigation, lightbox, SCORM reporting) belongs to later phases. Avoids building interaction behavior ahead of the phase that owns it.

2026-07-11 | `packages/player/src/lms/scorm2004.js` discovers `API_1484_11` via direct `window.parent` walking first, and falls back to an async `window.postMessage` bridge (relayed by `packages/launcher/template/scorm-api.js`) if direct discovery fails. As a consequence, every method that talks to the LMS (`initialize`, `getValue`, `setValue`, `commit`, `terminate`, and everything built on them) returns a Promise, even though the real SCORM `API_1484_11` interface itself is synchronous. | In the real dynamic-SCORM deployment, the player is served by the Mnemonify content server while the thin launcher is hosted by the LMS (SCORM Cloud/Ethos) — different origins. The Same-Origin Policy does not expose custom window properties like `API_1484_11` across origins (only a small allowlist: postMessage, location, close, closed, focus, blur, frames, length, top, parent, opener), so `window.parent.parent.API_1484_11` silently returns `undefined` from the player's iframe once the content server isn't same-origin with the LMS — this is exactly the "most common point of failure" flagged for Phase 2. Direct access still works for same-origin cases (e.g. local dev where everything runs on localhost), so it's tried first as a fast path; the bridge is what makes the real cross-origin deployment actually work. See [scorm-api.js](packages/launcher/template/scorm-api.js) for the launcher-side relay.

2026-07-11 | The `postMessage` bridge uses `targetOrigin: '*'` on both ends (player→launcher and launcher→LMS-adjacent responses), and validates incoming messages only by `event.source` identity and a `channel: 'mnemonify-scorm'` tag, not by origin. | The player doesn't know the launcher's origin in advance (it's whatever LMS/domain the author's course happens to be hosted on), so a strict `targetOrigin` allowlist isn't available without adding configuration. `event.source` matching is a partial mitigation (only the specific window we're talking to can respond), but this is not a hardened cross-origin channel. **Flagged for revisit in Phase 6 (security/deployment hardening)** — e.g. passing the expected launcher origin through `config.json`/URL params so `targetOrigin` can be tightened.

2026-07-11 | Phase 2 "page complete" detection is generic and block-type-driven, not course-specific: a page is considered viewed once every `knowledge-check` block on it has been answered (`onCorrect`/`onIncorrect` fired), or immediately if the page has none. | Phase 1/2 has no multi-page navigation, Continue button, or page-visit tracking yet (that's Phase 4's `player chrome` work per REQUIREMENTS.md Section 10). Since the sample course's single page has exactly one knowledge-check block, this makes `completion_rule: "viewed_all_pages"` fire once the learner submits it, and reports a real, non-trivial score/success to SCORM — needed to actually exercise and prove out the score/success/completion reporting path against SCORM Cloud. This heuristic will be replaced by real page-visit tracking (Continue button clicks, linear nav) in Phase 4; nothing here is hardcoded to `sample-course.json`'s specific block IDs or variable names.

2026-07-11 | Added `meta.completion_rule` (`"viewed_all_pages"` \| `"passed_final_quiz"`, optional) to `packages/schema/course.schema.json`, and set it on the sample course. No other ARCHITECTURE.md §3.3 meta fields (`nav_mode`, `page_groups`, `pdf_settings`, `utility_bar`) were added to the schema yet. | Phase 2 explicitly needed this one field ("Add this field to the sample-course.json meta block"); the rest belong to the phases that actually consume them (player chrome in Phase 4, PDF artifact in Phase 5) and adding them now would be schema surface with no corresponding implementation.

2026-07-11 | Ethos-specific SCORM quirks: not yet documented — Step 7 (uploading the launcher zip to SCORM Cloud, then Ethos, via an ngrok-tunneled content server) is manual and owner-executed, not run in this session. | REQUIREMENTS.md Open Question 4 stays open until that manual test happens. This entry will be updated with actual findings once Sebastin runs Step 7; the cross-origin bridge design above is the preparation for that test, not a substitute for it.

2026-07-12 | Fixed SCORM Cloud cross-origin iframe failure with two changes, not one. (1) `packages/server/src/index.js` now sends `Content-Security-Policy: frame-ancestors *` on every route so any LMS can iframe the content server, and CORS options were made explicit (`origin: '*'`, `methods: ['GET', 'OPTIONS']`, `allowedHeaders: '*'`). (2) `packages/player/src/lms/scorm2004.js`'s direct API discovery (`discoverDirectAPI`) is now wrapped in `try/catch`, and `App.jsx`'s `boot()` is too. | The requested `X-Frame-Options: ALLOWALL` is not a real header value (X-Frame-Options only defines `DENY`, `SAMEORIGIN`, and the deprecated/unsupported `ALLOW-FROM`); `Content-Security-Policy: frame-ancestors` is the standards-compliant equivalent and is what browsers treat as authoritative when both headers are present, so that's what got implemented instead. More importantly: headers cannot fix the actual reported console error at all. "Blocked a frame with origin X from accessing a frame with origin Y" describes the Same-Origin Policy blocking a cross-origin **window property read** (`current.API_1484_11` in the `window.parent` walk) — that boundary is not configurable via CORS, X-Frame-Options, or CSP; only `postMessage` (already built in Phase 2, see the 2026-07-11 bridge entry above) can cross it. The real bug was that this property read wasn't wrapped in a try/catch: if it throws instead of quietly returning `undefined` (WebKit/Safari has done this for cross-origin frame access), `initialize()`'s promise rejected uncaught, `boot()` in App.jsx (which also had no try/catch) never reached `setCourse(...)`, and the player was stuck on "Loading course..." forever — independent of any header. Added a regression test (`scorm2004.test.js`, "treats a cross-origin SecurityError... as not found") using a `Proxy` that throws on `API_1484_11` access, and a bundled-course fallback in `boot()`'s catch block so any future SCORM failure degrades gracefully instead of hanging. **Flagged for Phase 6 hardening** (joins the existing `targetOrigin: '*'` note): `frame-ancestors *` should be tightened to specific known LMS domains once those are known, rather than allowing any site to iframe the content server.
2026-07-12 | Editor drag-to-reorder (page blocks, carousel images) uses `@dnd-kit/core` + `@dnd-kit/sortable` rather than `react-dnd`. | `react-dnd` requires a backend (HTML5 or touch) chosen up front and wires through context providers and a `useDrag`/`useDrop` pair per element, which is more ceremony than a single-list reorder needs. `@dnd-kit` ships a `PointerSensor` that works out of the box for both mouse and touch, `SortableContext` + `arrayMove` cover the exact "reorder items in a list" case directly (`BlockCanvas.jsx`, `CarouselBlock.jsx`), and it has no dependency on the HTML5 drag-and-drop API, which is inconsistently supported in embedded/iframe contexts the editor might later run in. **Known gap:** the automation/testing environment used for this phase could not trigger `PointerSensor` via synthetic `PointerEvent` dispatch or the available computer-use drag tool, across repeated genuine attempts (page reorder, block reorder, carousel image reorder) — this was verified to be a testing-tool limitation, not an app bug, by confirming `handleDragEnd` → `arrayMove` → `onReorderBlocks`/`onChange` follows dnd-kit's documented pattern exactly and that the same code path saves correctly when triggered another way (e.g. page-level reorder was exercised via direct state updates). A real mouse/touch drag has not been manually verified by a human in a browser as of this entry — flagged for a manual smoke test before shipping.

2026-07-12 | Inline block text editing (`TextBlock.jsx`, `HeadingBlock.jsx`, `ListBlock.jsx`, etc.) uses native `contentEditable` divs synced imperatively via `useRef` + `useEffect([block.block_id])` on mount/block-change, with content committed to React state only `onBlur` — never via controlled `value`/`onChange` re-rendering React children into the editable region on every keystroke. | Controlling a `contentEditable` element's children from React state on every keystroke fights the browser's own cursor/selection management — the DOM node React reconciles is the same node the user is actively typing into, so a naive controlled re-render resets cursor position after each character. Syncing imperatively only when the block identity changes (not on every keystroke) and reading back the final text on blur avoids this entirely, at the cost of the block's displayed text lagging one blur behind the true content model during active editing (acceptable since nothing else reads block content while it's focused). **Known limitation:** the Bold/Italic/Underline toolbar in `TextBlock.jsx` uses `document.execCommand`, which is deprecated but still the only zero-dependency way to get live WYSIWYG formatting inside a `contentEditable` region without a rich-text editor library. Only `textContent` (plain text) is persisted on blur — bold/italic/underline markup is visible while editing but does not survive a save/reload, because the `rich_text` content shape (`[{t: 'text', v: string}]`, decided 2026-07-11) has no field to store inline formatting spans. Fixing this properly means extending `rich_text` to a run-based format (e.g. `{t, v, bold, italic}[]`) and is deferred — out of Phase 3 scope, flagged for whichever phase does richer authoring. **Second known limitation, found during the Phase 3 integration test:** automating `contentEditable` edits via browser tooling only works with a real click + type + blur sequence targeted by a freshly-read element reference; a synthetic `focus()`+`blur()` call issued in the same script tick does not reliably fire React's `onBlur` (the browser appears to coalesce same-tick focus/blur), and raw screenshot-pixel coordinates do not reliably hit the right element because screenshot pixel space and the real viewport are not always the same scale. Neither is an app bug — both are testing-environment quirks — but they cost significant back-and-forth during manual verification and are recorded here so future sessions don't rediscover them from scratch.

2026-07-12 | Word import parsing (`packages/server/src/lib/htmlTableParser.js`) is a small hand-written regex-based `<table>`/`<tr>`/`<td|th>` extractor over mammoth.js's HTML output, rather than a full DOM parser (e.g. `jsdom` or `linkedom`). | `mammoth.js` converts `.docx` to a small, predictable HTML subset (mammoth does not emit malformed markup, nested tables, or attributes beyond what its own conversion rules produce), and the only thing the import pipeline needs to extract is exactly one table's rows and cells plus HTML-entity-decoded text — a full DOM implementation is unnecessary weight for that. The parser is explicitly scoped to mammoth's own output format, not general HTML, and would need to be swapped for a real parser if the input source ever changed (e.g. accepting arbitrary uploaded HTML rather than mammoth-generated HTML). The reverse direction (`wordExport.js`, JSON → `.docx`) uses the `docx` npm package rather than hand-rolling OOXML, since generating valid Office Open XML by hand is not a reasonable scope-of-effort tradeoff the way a small custom parser is.

2026-07-12 | The `courses` table's `course_json` column is `JSONB NOT NULL DEFAULT '{}'` at the database level (`003_courses.sql`), but the editor never actually sends a bare `{}` — `packages/editor/src/lib/blockDefaults.js`'s `createBlankCourseJson()` always builds a schema-valid document (`schema_version`, `meta.course_id`/`title`/`theme`, empty `variables`/`assets` arrays, one empty page) before `POST /api/courses`. | This was found as a real bug during Phase 3 integration testing, not a deliberate design choice being documented after the fact: both "New Course → Blank" and the onboarding tour's "Start guided tour" (which silently creates a course behind the scenes) originally sent `course_json: {}` straight through, and `CourseEditor.jsx`'s `activePage()` calls `json.pages.find(...)` with no guard — an empty object has no `.pages`, so the whole React tree unmounted with a blank white screen and no visible error. The DB-level `'{}'` default exists only as a safety net against a `NOT NULL` constraint violation if some future code path posts without the field at all; it is not meant to be a valid document on its own, and `course.schema.json` requires `pages` to have `minItems: 1`. The fix is application-layer (always construct a valid document before the request leaves the client) rather than DB-layer, since the schema's actual validity requirements (matching `course_id` pattern, non-empty title, valid theme accent) can't be expressed as a Postgres column default anyway.

2026-07-12 | Fixed a QA-reported image-upload hang with three coordinated changes rather than one: (1) `packages/server/src/routes/assets.js`'s `POST /assets/upload` handler now wraps its body in try/catch, and `packages/server/src/index.js` gained a global Express error-handling middleware (registered last) as a safety net for middleware-level errors (e.g. multer's file-size limit) that fire via `next(err)` before a route handler runs. (2) `packages/editor/src/components/blocks/ImageBlock.jsx`'s `handleFileChange` now wraps the upload in try/catch/finally, always clearing `uploading` and showing an inline error message on failure. (3) `CourseEditor.jsx`'s `doSave()` (autosave) got the same try/catch treatment. | Express 4 does not catch a rejected promise thrown from inside an `async (req, res) => {}` route handler — if `insertAsset`'s `pool.query` ever rejected, the request produced no response at all and just hung, and since the editor's axios client has no configured timeout, the client-side `await api.uploadAsset(...)` hung right along with it, leaving `uploading` stuck `true` forever with no way to recover short of a page reload. Root cause was purely missing error handling, not anything upload-specific. **The image block is only added to `course_json` after the upload actually resolves successfully** (this was already true for the happy path before this fix; the fix makes it true for the failure path too, by ensuring failures now surface as a caught, displayed error instead of leaving execution in limbo). A second, unrelated correctness bug was found and fixed in the same file while investigating: `ImageBlockEditor` was generating its own client-side `asset_id` via `genAssetId()` instead of using the server-returned `uploaded.asset_id` from `insertAsset` — meaning a single-image upload's `assets` table row and its `course_json.assets` entry never shared the same `asset_id`, silently breaking any code that looks an asset up by id across both layers (e.g. `MediaLibraryPanel`'s alt-text sync). Now uses `uploaded.asset_id` directly, matching the pattern `MediaLibraryPanel.jsx`'s bulk-upload path already used correctly.
2026-07-12 | `packages/player/src/blocks/ImageBlock.jsx` now renders a visible "Image unavailable" placeholder (not `return null`) when `block.content.asset_id` is missing, `null`, or doesn't resolve to any entry in `assets`. | Previously the block silently rendered nothing, which technically never crashed but gave no visual indication anything was wrong — an image block stuck mid-upload (or one whose asset was deleted from the media library after the block was created) just vanished from the page with no author-visible signal. A dashed-border placeholder box matches the existing look of an empty accordion/list item and makes the broken state discoverable without touching any other block type's fallback behavior.
2026-07-12 | Added `data-placeholder` text to all nine contentEditable fields across `AccordionBlock.jsx` (title, body), `ListBlock.jsx` (item), `KnowledgeCheckBlock.jsx` (question, answer option), `TabsBlock.jsx` (tab label, tab body), `TextBlock.jsx` (body), and `HeadingBlock.jsx` (heading text), using the existing `.editable-field:empty::before { content: attr(data-placeholder); ... }` CSS rule in `courseEditor.css` rather than introducing a new one. | The CSS mechanism already existed (added during Phase 3 block-editor work) but no field actually carried a `data-placeholder` attribute, so every empty contentEditable rendered as an unlabeled grey box — exactly the "invisible until mouseover" bug QA reported. The existing rule doesn't use `position: absolute` on the pseudo-element (unlike the pattern QA's report suggested): since `:empty` only matches a contentEditable div with zero child nodes, there's nothing for the placeholder text to overlap, so it renders safely in normal inline flow and no `position: relative` container wrapper is needed. Kept the existing `.editable-field` class-scoped selector (`.editable-field:empty::before`) rather than switching to a bare `[contenteditable]:empty::before` — it's already applied to every field in scope here, and staying scoped to an explicit class avoids accidentally styling some future non-block contentEditable element (e.g. a future rich-text toolbar) that isn't meant to show a Click-to-edit placeholder. Kept `var(--color-text-muted)` (`#5b6572`) for the placeholder color instead of a new hardcoded `#9CA3AF`, to stay consistent with the app's existing design-token system rather than introducing a one-off value.
2026-07-12 | `packages/player/src/blocks/BlockRenderer.jsx`'s fallback for an unregistered block type now special-cases `carousel` in editor preview only: it renders "Carousel preview not available — renders correctly in published course" when `isPreview` is true (a new prop threaded down from `App.jsx`, computed from the same `?preview=true` URL param `App.jsx`'s `boot()` already reads), and falls through to the pre-existing silent `console.warn` + `return null` behavior otherwise. | Carousel player rendering is Phase 5 scope, not this fix. But telling the author "this will work once published" is only true in editor preview — showing that same reassurance to an actual learner in a real SCORM/standalone session would be false today, since carousel genuinely doesn't render there either yet. Scoping the message to `isPreview` keeps the promise accurate for both audiences: authors get an explanation instead of a block that looks silently deleted, while the generic silent-skip fallback (which every other not-yet-implemented block type still relies on) is left completely unchanged for any non-preview context.

2026-07-12 | Brand applied to both `packages/editor` and `packages/player` via a single `brand.css` per app (`.../src/styles/brand.css`), imported before every other stylesheet, defining all color/typography/spacing/radius/shadow values as `:root` CSS custom properties. Every existing color in both apps' CSS was migrated to reference these tokens (or, for the handful of literal values the brand brief itself specified directly — status-badge colors, top-bar `rgba(255,255,255,*)` hover states, the onboarding backdrop's `rgba(15,23,42,0.7)` — kept as literals since that's what was explicitly asked for, not a token gap). The old `tokens.css` in each package was trimmed down to only the handful of values brand.css has no concept of and that are genuinely structural, not brand: panel widths in the editor, line-height and the mobile-first responsive `--text-*` breakpoint overrides in the player (the responsive type scale is a hard requirement per ARCHITECTURE.md Section 5 / REQUIREMENTS.md P0-2, verified at 375/768/1280px, and had to be preserved through the migration, not dropped). | Single source of truth for all colors and typography: updating one token in `brand.css` updates the entire app, consistent with the architecture principle of no hardcoded values. Old and new token names mostly didn't collide (`--text-*`, `--space-1..6`, `--radius-sm` are spelled identically in both systems, so swapping the imported file was often enough on its own), but where they did collide with a *different* meaning under the same old name (e.g. the old `--color-bg` meant "white card surface" in practice, which is brand's `--color-surface`, not brand's own `--color-bg` which means "page background") each usage was individually re-mapped by what it actually rendered as, not by name-matching alone. One non-cosmetic fix was required to do this safely: the player's `--color-accent` was not a static brand color at all -- it's the per-course customizable interactive color (`meta.theme.accent`, ARCHITECTURE.md 3.3), overridden at runtime by `App.jsx` via `style.setProperty`. Renaming every `var(--color-accent)` usage in player.css to `var(--color-primary)` without also moving the runtime override target would have silently broken that feature (the override would keep writing to a variable nothing reads anymore). Fixed by renaming the override target itself (`App.jsx` now sets `--color-primary`, not `--color-accent`), which only touches an internal CSS variable name -- the documented JSON schema field is still `meta.theme.accent`, unchanged. This left brand.css's own fixed `--color-accent` (#F59E0B, amber) free for its intended purpose: a rare, deliberate CTA color, not the everyday interactive color.
2026-07-12 | Logo processing (`packages/server/scripts/process-logos.js`): removes the dark halo/glow-bleed from the three brand source PNGs, using a per-pixel rule that treats a pixel as background if its brightest RGB channel is below a threshold (final value: **100**, tuned empirically from 60/80/90/100/110) *and* it isn't hue-identifiable as the dark navy wordmark (protected whenever blue meaningfully exceeds both red and green, regardless of brightness). | The brief's original spec (checking alpha < 200) turned out to not apply to these files at all: the source PNGs have **no real alpha channel** -- every pixel is opaque (alpha=255) and the "transparent" checkerboard seen when previewing them is baked directly into the RGB data as a literal checker texture, not real transparency. A brightness-only rule (checking if R, G, and B are all below a threshold) was also tried and rejected: sampled pixel data showed the dark navy wordmark (~13,24,49) is exactly as dark as the checkerboard (~15,15,15 / ~79,80,80), so a pure-darkness rule would erase the wordmark right along with the background at any threshold that actually clears the checkerboard. The fix that worked: separate on hue, not just brightness -- protect any pixel where blue clearly dominates red and green (matches the wordmark's navy) independent of how dark it is, and remove any other pixel below the brightness threshold (catches both the neutral-gray checkerboard and the cyan-tinted halo, which share no hue with navy). Threshold 100 was the smallest value that fully cleared the checkerboard on the wave/neuron artwork without touching the wordmark. A residue of full-resolution haze remains specifically in the spark/bloom glow effect near the dendrite tips -- that soft glow has no clean brightness separation from the background at any single global threshold, since it's a continuous gradient from bright spark down to background level. This was verified to be imperceptible at the sizes these assets are actually displayed (36px-tall top-bar lockup, 32x32 favicon), so it wasn't chased further with more complex per-region processing.

2026-07-13 | `CourseEditor.jsx`'s "Export Word" control is now a button that awaits an explicit `saveNow()` before navigating to `GET /api/templates/:id/export-word`, rather than a plain `<a href>` that navigated immediately. A "Saving before export..." label replaces the button text if that save takes longer than 500ms. | Root-caused via the logging QA asked for (`[word-export] course_id=... pages=... blocksOnFirstPage=...` in `routes/word.js`): the endpoint was already reading `course_json` fresh from Postgres on every request, and `buildTemplateWordDoc` was already faithfully transcribing whatever it found there -- neither of the two hypothesized causes (stale DB read, docx generation dropping content) held up under inspection. The actual bug was a client-side race: templates are edited through the same `CourseEditor.jsx` as regular courses (a template is just a course row with `is_template = true`), autosave is debounced 5 seconds after the last edit (`AUTOSAVE_DELAY_MS`), and the old `<a href>` export link had no way to intercept a click and wait -- so editing a field and clicking Export Word inside that 5-second window exported whatever was in the DB *before* the edit. Reproduced directly: edited a heading's text, immediately clicked Export Word, and the downloaded .docx contained the pre-edit placeholder text; confirmed via `read_network_requests` that the `PATCH` fired only once `saveNow()` was awaited, and that the `GET .../export-word` request did not fire until after it resolved. `saveNow()` (previously fire-and-forget) now returns the `doSave()` promise so callers can await it -- existing fire-and-forget callers (e.g. the preview-mode toggle) are unaffected since they simply don't await the returned promise. Note: `TemplateLibrary.jsx`'s own "Export Word" link (on the templates list page) was not touched -- it has no locally-edited, not-yet-saved `course_json` in memory to race against, so it isn't exposed to this bug.

2026-07-16 | Replaced the initial brand (Ocean Teal/Deep Navy/Amber, Pomelli-sourced neuron logo) with a new brand kit (Deep Navy/Primary Blue/Violet gradient, symmetrical pathway-network M mark), delivered as `brand/Mnemonify-Brand-Kit/`. New `--mn-*` tokens (`packages/{editor,player}/src/styles/mnemonify-tokens.css`) are aliased through the existing `--color-*`/`--font-*` variable names inside each app's `brand.css`, rather than a find-and-replace sweep of every component file. | The original logo did not scale well to small sizes (favicon, app icon); the new mark is geometrically symmetrical (outer node centers form a square) and reads clearly at 16px. Aliasing through the existing `--color-*` names means every component that already references `--color-primary`, `--color-dark`, etc. (buttons, cards, top bar, form inputs, block styles) inherits the new brand automatically, with zero per-component changes and zero risk of a missed hardcoded value that a rename sweep could leave behind. The top bar logo (`CourseLibrary.jsx`, `TemplateLibrary.jsx`) now uses the reversed horizontal SVG at `width: min(100%, 200px); height: auto` -- width-constrained only, per the brand guide's rule against setting both dimensions on a non-square container. `--color-error` (#EF4444) and `--color-surface` (#FFFFFF) have no `--mn-*` equivalent in the brand kit and stay as literals, unchanged from the prior brand pass. Body font moves from Roboto to Inter (`--mn-font-ui`); heading font stays DM Sans (`--mn-font-display`), now loaded at weight 800 in addition to the existing weights.

2026-07-16 | Phase 3.5 Part 1 schema hooks: added `meta.header`/`footer` (rich_text_field), `meta.page_numbering`, `meta.objectives`/`concepts`, block-level `faculty_notes`/`objective_ids`/`concept_ids`/`include_in_pdf` to `packages/schema/course.schema.json`; added a new `reflection` block type (no schema change needed since `block.type` was already an unconstrained string and `block.content` an unconstrained object); added answer-level `feedback` (`{rich_text, image_id, reference_ids}`) to knowledge-check options and a `caption` field shape for future table blocks, neither of which required a schema change for the same reason. `samples/sample-course.json` was updated to demonstrate every new field and validated against the updated schema with `ajv-cli` (`samples/sample-course.json valid`). | Table block itself (Section 3.7) was explicitly named as Part 2 scope in this task's own framing ("Do not start on Part 2 items"), and no table block exists anywhere in the codebase yet (no `TableBlock.jsx` in editor or player, no `table` type in the sample course) -- so Step 1d/2d/3d's table-caption sub-items were skipped entirely rather than half-building a caption UI for a block type that doesn't exist. `include_in_pdf` was added to the block schema even though it wasn't explicitly listed in Step 1's bullet points, because the reflection block's own spec example in this task includes `"include_in_pdf": true` as a top-level block field, and ARCHITECTURE.md 3.2 already documented this field as existing -- it had simply never been added to the schema until now.
2026-07-16 | Editor undo/redo (`packages/editor/src/pages/CourseEditor.jsx`): implemented as two in-memory-only stacks (`undoStackRef`/`redoStackRef`, capped at 50 via `MAX_UNDO_STACK`) built on top of the pre-existing immutable-state pattern -- `updateCourseJson` already produced a new `course_json` object per mutation via spreads (confirmed no in-place mutation existed anywhere in the mutation handlers), so no state-management refactor was needed before adding history tracking. A single choke point (`updateCourseJson`) is the only place that pushes undo snapshots; `handleUndo`/`handleRedo` apply snapshots via a separate `applyRestoredJson` that deliberately bypasses `updateCourseJson` so restoring history can never itself push a new undo entry. | Debouncing: text-typing fields (course title, header/footer, faculty notes) fire `onChange` on every keystroke, and coalescing a burst of keystrokes within `TYPING_BURST_MS` (500ms) into one undo snapshot is what makes undo revert a meaningful edit instead of one character. But applying that same time-window burst check uniformly to every mutation had a real bug, caught during this session's own manual testing: two genuinely separate discrete actions (duplicate a block, then toggle a settings checkbox) fired within 500ms of each other -- easily achieved by fast/scripted interaction, and in principle possible from a fast human double-action too -- coalesced into a single undo step, silently merging two unrelated actions. Fixed by adding a `forceSnapshot` option to `updateCourseJson`, set to `true` from every block-structure and page-structure handler (`handleAddBlock`, `handleDeleteBlock`, `handleDuplicateBlock`, `handleReorderBlocks`, `handleAddPage`, `handleRenamePage`, `handleDeletePage`) -- these always get their own snapshot regardless of timing, matching ARCHITECTURE.md 3.9's listing of them as unconditional undo steps, distinct from the debounced text-edit case. Verified the fix directly: duplicate-then-immediately-delete (fired back to back with no pause) now produces two independently-undoable steps, not one. Asset uploads are correctly excluded from undo's effect on the file system: undo/redo only ever swap the in-memory `course_json` value and never call any asset-deletion API, so an uploaded file always remains in the media library even if undoing the block that referenced it also reverts the `assets` array entry alongside it (both changes were pushed as part of the same mutation that added them). Keyboard shortcuts (Cmd/Ctrl+Z, Cmd/Ctrl+Shift+Z) are suppressed while focus is inside any input/textarea/select/contentEditable element, deferring to the browser's own native undo for in-progress, not-yet-committed keystrokes in that field; app-level undo/redo takes over once focus leaves. Manually verified end to end: typing+undo (single meaningful step), add+undo+redo, delete+undo (content and settings restored), an 8-action sequence undone all the way back to the empty-page start state and redone all the way forward with the document matching at every step, and undo-twice-then-new-change correctly clearing the redo stack. Drag-reorder+undo was not manually verified in this session for the same pre-existing reason noted in the 2026-07-12 `@dnd-kit` entry (browser automation cannot trigger `PointerSensor` via synthetic events) -- flagged again here for a human smoke test.
2026-07-16 | Confirmed: `packages/player/src/blocks/BlockRenderer.jsx` never passes `block.faculty_notes` to any block component, in any player context (SCORM, standalone, preview, review) -- it is not read at all, not merely hidden from view, per the hard rule in ARCHITECTURE.md 3.8.
2026-07-16 | Confirmed: the reflection block (`packages/player/src/blocks/ReflectionBlock.jsx`) holds learner-typed text in local React component state only. It is never sent to any API, never included in `App.jsx`'s SCORM `suspend_data` serialization (which only ever contains the trigger-engine `variables` object and `pageId`), and never attached to any analytics/telemetry event -- there is no code path connecting the textarea's value to anything outside the component. Matches REQUIREMENTS.md P1-46's local-only privacy requirement exactly.

2026-07-16 | Fixed the course editor top bar breaking with a long course title: found via manual repro (a 175-character title) that the reported symptom ("Undo/Redo buttons stretch") wasn't actually button-stretching -- `getBoundingClientRect()` measurements showed the Undo/Redo/Phone/Tablet/etc. buttons stayed compact and correctly vertically centered throughout, since `.top-bar` already had `align-items: center` (not the default `stretch` the bug report assumed). The real bug: the title `<h1>` has no `white-space`/`overflow` constraint, so once it needs more than ~2 lines it renders taller than the bar's fixed `height: 56px` and overflows above and below it (a flex item's content isn't clipped by a height-constrained flex container unless `overflow: hidden` is set) -- from a glance this reads as "things are stretched/broken" even though no element's box actually grew. Fixed with single-line truncation (`white-space: nowrap; overflow: hidden; text-overflow: ellipsis`, plus `min-width: 0` so the flex item can actually shrink below its text's intrinsic width) and a `title` attribute for the full text on hover, per the task's own fallback recommendation. Also gave `.course-editor__history-controls button` an explicit `width/height: 32px` so Undo/Redo have a guaranteed fixed footprint regardless of any future layout change, not just whatever their icon glyph and padding happen to produce. | The title truncates rather than wraps: a two-line title in this fixed-56px bar is exactly the "recurring source of layout bugs" the task called out, and truncation with a hover tooltip preserves full access to the title (click-to-edit still works normally) without ever risking overflow again, regardless of how long a future title gets.
2026-07-16 | Image block gained `content.width_preset` (small/medium/large/full → 25/50/75/100%) and `content.alignment` (left/center/right, ignored when full), rendered via a `--image-width-pct` CSS custom property set inline per-block (`width: var(--image-width-pct, 100%)` with `max-width: 100%` as an outer safety cap) rather than fixed pixel widths or a freeform drag-to-resize handle. Editor Settings panel gets Small/Medium/Large/Full and Left/Center/Right button groups (Alignment disabled when Full is selected); changes are forced (non-debounced) undo/redo snapshots via a new `forceSnapshot` option threaded through `SettingsPanel`'s `onChangeBlock` prop into `CourseEditor.jsx`'s `handleChangeBlock`, so a discrete Size/Alignment click always gets its own undo step (matching how block/page structural actions already work) rather than being coalesced with adjacent settings edits. On viewports under 768px every preset floors at 60% width (`width: max(var(--image-width-pct), 60%)`) so a "small" image on a phone never becomes illegibly tiny. | Percentage-based presets, not pixels or freeform resize, because ARCHITECTURE.md's block-stacking model has no fixed canvas width to measure pixels against in the first place (mobile-first, no pixel positioning, ever) -- a pixel width would either overflow on a phone or look arbitrarily small on a wide desktop reading column, and freeform drag-resize is exactly the per-block pixel-canvas escape hatch the architecture explicitly rules out (REQUIREMENTS.md Non-Goal 1). A small fixed set of presets (rather than an arbitrary 1-100% slider) keeps every author's course visually consistent and keeps the settings UI a simple button group instead of a slider needing its own drag-precision handling. Print/PDF rendering was not given a separate override: no print stylesheet or Puppeteer pipeline exists in the codebase yet (Phase 5), and since the width is applied via inline style rather than a screen-only `@media` rule, it will already carry through unchanged into whatever print/PDF rendering context consumes this component later -- there was nothing to add without inventing print infrastructure ahead of the phase that owns it.

2026-07-16 | Phase 3.5 Part 2: added `two_column`, `table`, and `embed` block types end to end (schema, player, editor). Two-column slot content is restricted to `text`/`heading`/`image`/`embed` via a dedicated `inner_block` schema definition (deliberately excluding `two_column` itself), and inner block_ids are namespaced as `${parentBlockId}_left` / `${parentBlockId}_right` (e.g. `blk_col1_left`) rather than independently random ids, both enforced in `packages/editor/src/lib/blockDefaults.js`'s `createInnerBlock`/`createBlock` and mirrored when regenerating ids for copy/move/template-insert (`CourseEditor.jsx`'s `assignBlockIds`). The column divider (`TwoColumnBlock.jsx` editor) snaps to 5% increments between the 25-75% range on drag, committing only on mouseup as a single forced undo snapshot (live drag position is local React state, not pushed to the document). Embed blocks sandbox their iframe (`sandbox` content field, default `allow-scripts allow-same-origin allow-popups`) and explicitly strip `allow-popups-to-escape-sandbox` if ever present, in both player and editor preview. | Slot types are restricted to a flat, non-nestable set (no `two_column` inside `two_column`) because ARCHITECTURE.md's block model has no general-purpose container/grouping concept -- two-column is a single, fixed one-level split, not a layout primitive, so allowing recursion would require solving arbitrary nesting (selection, drag-reorder, undo scoping) for a feature nobody asked for. The parent-namespaced inner block_id convention was chosen over random ids so that copying/moving/duplicating a two-column block (or regenerating ids on template insert) can regenerate a slot's id deterministically from the new parent id without an extra id-mapping pass just for slots specifically -- though in practice the id-remap code (`assignBlockIds`) still special-cases `two_column` to preserve this convention on regeneration, since a random id would otherwise be perfectly correct too. 5% (not 1% or freeform) matches the same "no freeform pixel/percent canvas" philosophy as the image `width_preset` decision (2026-07-16, image block entry): coarse, predictable snap points keep every author's split visually consistent instead of drifting to odd values like 37% vs 38% that add no real value. `allow-popups-to-escape-sandbox` is specifically stripped because it is the one sandbox token that lets a popup opened from inside the iframe escape the sandbox's restrictions entirely and navigate/control the parent window -- every other allowed token stays contained within the iframe itself, which is the whole point of the in-player containment rule (ARCHITECTURE.md 5.2).

2026-07-16 | Move/Copy block between pages (`CourseEditor.jsx`'s `handleMoveBlockToPage`/`handleCopyBlockToPage`, UI in `BlockCanvas.jsx`'s `BlockWrapper` toolbar + `MoveCopyBlockModal.jsx`): Move relocates the exact same block object (preserving block_id, trigger_ids, and all settings unchanged) so no id remapping is needed or wanted. Copy deep-copies via a two-pass `assignBlockIds`/`rebuildBlockWithIds` pair: first every block_id in the copied subtree (the block itself, plus two-column slots and any nested `body_blocks` under accordion/tabs/carousel items) gets a freshly minted id in one shared map, then the tree is rebuilt with new block_ids, new trigger_ids, and any action `target` that pointed at another id *within that same subtree* rewritten to the new id. A target pointing outside the copied subtree (a different block on the page, not part of the copy) is left as-is, since there is no copy of that block to point to instead. The identical two-pass helper is reused for "Insert from template" (`regeneratePageIds`), just seeded with every block on the template's page instead of one block's subtree, so cross-block trigger references *within* a single templated page (e.g. an accordion item's trigger targeting another block on the same page) are correctly remapped too -- something a plain per-block copy can't do, since it only sees one block's own ids. Both Move and Copy force their own undo/redo snapshot. | Doing the id-assignment and id-rewriting as two separate passes (rather than rewriting inline as each block is visited) is what makes cross-block-within-the-same-copy trigger references resolve correctly: a single-pass approach would rewrite a trigger's target before knowing what that target's new id would end up being, if the target block happens to be visited later in traversal order. This is a deliberately different (and more thorough) fix than plain single-block `Duplicate` (`regenerateIds`, unchanged from earlier phases), which still does not remap trigger targets at all -- that gap was not introduced by this work and was left alone rather than silently changing established Duplicate behavior mid-task; it's called out here as a known pre-existing gap, not a regression.

2026-07-16 | Page templates (personal/org-scoped, reusable across courses): new `page_templates` table (migration `006_page_templates.sql`) and `POST`/`GET /api/page-templates` routes, scoped by `organisation_id` only (no user-level personal/org distinction beyond the `scope` label field itself, since Phase 3 has no auth -- matches the existing `DEV_ORG_ID`/`DEV_USER_ID` pattern used everywhere else in this phase). Saving strips `page_id` from the stored `page_json` (a template is a page *shape*, not a specific instance) and "Insert from template" always mints a fresh `page_id` plus fully regenerated block_ids via the same `regeneratePageIds` helper Copy-to-page uses, inserted immediately after the currently active page as a forced undo/redo snapshot. | While building and testing this feature, `SavePageAsTemplateModal.jsx`'s save button was found to get stuck permanently disabled with no visible error if the POST failed (the `await` was unguarded, so a thrown request left `busy` stuck `true` forever) -- fixed with a try/catch/finally so `busy` always resets and a failure now surfaces an inline error message instead of a silently-frozen modal. This is a *new* component written in this session, not a pre-existing one, so it was fixed directly rather than just logged as a known gap the way the Duplicate/trigger-remap gap above was.

2026-07-16 | Images in knowledge check: added `content.question_image_id` (block-level) and `image_id` on each option object, matching ARCHITECTURE.md 3.8's documented (but previously unbuilt) shape exactly -- including reusing the already-reserved-but-previously-unused `feedback.image_id` field on per-option feedback. Block-level `correct_feedback`/`incorrect_feedback` gained sibling `correct_feedback_image_id`/`incorrect_feedback_image_id` fields instead of converting those two fields to the `{rich_text, image_id}` object shape ARCHITECTURE.md shows, since both fields are still plain strings in this codebase's established (simplified, Part-1-era) knowledge-check content model, not rich_text objects -- adding a sibling field preserves that existing simplification instead of partially adopting the fuller architecture shape for just these two fields. All four image pickers (question stem, per-option, per-option feedback, block-level feedback) reuse the existing `MediaLibraryPanel` in its multi-select mode, taking only the first selected id -- deliberately not built as a true single-select variant, since the spec called for "single select" behavior, not a new picker component, and this achieves that behavior with zero new UI code. | Verified end to end in the browser, not just read from code: uploaded/selected images render correctly above the question stem and above an option's label in both the editor and the actual player (including the correct-answer feedback path when an option has no per-option feedback image, correctly falling back to the block-level `correct_feedback_image_id`).

2026-07-16 | Superscript/subscript: added X²/X₂ toolbar buttons to `TextBlock.jsx` and `KnowledgeCheckBlock.jsx` editors using `document.execCommand('superscript'/'subscript')`, identical pattern to the existing Bold/Italic/Underline buttons in the same toolbars. Confirmed player CSS applies no reset/override to `<sup>`/`<sub>` (relies on browser UA defaults). | Inherits the same known, already-documented limitation as Bold/Italic/Underline (2026-07-12 DECISIONS.md entry): `TextBlockEditor`'s content model only ever persists plain `textContent` on blur, not the DOM's HTML, so formatting applied via `execCommand` is visible live while editing but does not survive a reload -- this is a pre-existing content-model gap, not something newly introduced or newly discovered by adding two more buttons that use the exact same mechanism as three buttons already shipped. Not fixed here, consistent with treating the underlying rich-text persistence gap as out of this task's scope, same as it was for B/I/U.

2026-07-16 | Discovered (not fixed) during manual undo/redo testing: contentEditable fields built on the "sync DOM to a ref only via a `useEffect` keyed on `block.block_id`" pattern (`TextBlock.jsx`, and by the same underlying React-vs-contentEditable limitation, `KnowledgeCheckBlock.jsx`'s inline-rendered fields) do not visually re-sync when their content changes via Undo/Redo -- the underlying document data reverts correctly (confirmed via re-save and via a full page reload showing the correct, reverted text), but the on-screen field itself keeps showing the pre-undo text until the block is remounted (page switch, or a hard reload). | This is a pre-existing architectural limitation, not introduced by Part 2 -- the block_id-only-keyed sync effect already existed for ordinary editing before this phase. It is being logged here rather than fixed because a broad fix (re-syncing on every content change) risks reintroducing the exact cursor-jumping bug that pattern was presumably written to avoid in the first place, and doing it safely for every contentEditable field in the app is a larger, cross-cutting change than this task's scope. Flagged for a future pass, same spirit as the existing 2026-07-12 B/I/U-persistence entry.

2026-07-17 | **Root cause found and fixed for the rich-text formatting data-loss bug** (QA-reported: bold/line-breaks visible while editing, gone after Preview is closed): every contentEditable field across the editor (`TextBlock.jsx`, `HeadingBlock.jsx`, `ListBlock.jsx`, `AccordionBlock.jsx`, `TabsBlock.jsx`, `KnowledgeCheckBlock.jsx`, and table cells added in this same fix) was capturing `e.currentTarget.textContent` on blur, not `innerHTML`. `textContent` strips every tag (destroying bold/italic/underline/sup/sub) *and* concatenates all text nodes with zero separator character -- not even a space -- across `<div>`/`<br>` element boundaries, which is why line/paragraph breaks were destroyed too, not just inline formatting. This is a strictly more severe finding than the 2026-07-12 "B/I/U doesn't persist" entry and the 2026-07-16 undo/visual-sync entry immediately above assumed: neither of those entries clocked that line breaks were also being destroyed, and the 2026-07-16 entry's "the underlying document data reverts correctly" claim was only ever checked against plain-text content, where `textContent` capture happens to be lossless. **This entry supersedes both.** Fix: replaced `textContent` capture with a from-scratch, dependency-free HTML sanitizer (`packages/{editor,player}/src/lib/richText.js`, duplicated per the existing cross-package-duplication precedent, not cross-imported) that parses the field's `innerHTML` into a small allowlisted AST (`B`/`STRONG`/`I`/`EM`/`U`/`SUP`/`SUB`/`BR`, zero attributes ever copied on any tag -- this is what makes it XSS-safe without needing a separate attribute-level allowlist) and serializes it back to canonical HTML, stored in `rich_text` as a new `{t: 'html', v: string}` segment type (old `{t: 'text', v}` / `{t: 'asset_link'}` segments still render correctly for backward compatibility). The player renders this via a new `RichText.jsx` component that walks the same sanitized AST into real React elements -- never `dangerouslySetInnerHTML` -- per ARCHITECTURE.md 19's no-raw-HTML-injection rule. A new shared `EditableRichField.jsx` component replaces each block's hand-rolled contentEditable wiring, with one deliberate side effect: its DOM-sync `useEffect` is keyed on the *sanitized value itself*, not on `block.block_id` -- this is what actually resolves the 2026-07-16 undo/visual-sync entry (the field now re-syncs correctly on undo/redo and page-switch, not just on block identity change), so that limitation is also closed by this fix, not merely superseded on paper. | The bug was never a "visual sync" issue as originally triaged -- `textContent` on blur permanently discards the DOM's actual formatting and structure before it's ever written to `course_json`, so no amount of re-rendering or re-syncing could have recovered it; the data was gone at capture time, not lost in some later re-render. Chose a hand-rolled sanitizer over a rich-text editor library (Slate, TipTap, Quill) to stay within the existing zero-new-dependency, `document.execCommand`-based toolbar pattern (2026-07-12 entry) rather than a much larger editor-model migration that wasn't in scope for a bug fix. Verified end to end in the browser: typed multi-paragraph text with bold and a subscript in a plain `TextBlock` (not a two-column slot), opened and closed Preview, did a genuine hard reload (fresh navigation from the course library, not a cached in-session state), and confirmed both the formatting and the paragraph break survived -- and confirmed the player's rendered DOM (`<p>First <b>paragraph</b> of the case.<br>Second paragraph mentions del(<sub>5q</sub>) finding.</p>`) matches exactly.

2026-07-17 | **Root cause found and fixed for two-column slot image upload failing** ("Upload failed. Please try again." on every file type, only inside two-column slots): `TwoColumnBlockEditor`'s function signature was `({ block, onChange })` -- it silently dropped the `assets`/`courseId`/`onAddCourseAsset`/`onAddCourseAssets`/`onUpdateCourseAsset` props that `BlockCanvas.jsx`'s `BlockWrapper` *was* already passing down to it, a classic React prop-drilling gap (a parent passing a prop doesn't help if the child's destructured signature doesn't accept it). Traced the actual failure chain, not just guessed at it: the nested `ImageBlockEditor` received `courseId={undefined}`; its upload `FormData.append('course_id', courseId)` stringifies a JS `undefined` to the literal string `"undefined"`, which is truthy and passes the server's naive `if (!course_id)` check in `routes/assets.js` -- so the upload actually *succeeded* server-side, creating a real, orphaned asset row associated with a literal `"undefined"` course id. The failure the user saw happened client-side, one step later: `ImageBlockEditor`'s post-upload success handler calls `onAddCourseAsset(assetEntry)`, which was also `undefined` for the same reason, so calling it as a function threw, was caught by the generic catch block, and surfaced as "Upload failed. Please try again." even though the file had genuinely uploaded. Fixed by threading all five props through the full slot component chain (`TwoColumnBlockEditor` → `Slot` → `SlotContent` → the slot's `<Editor>`), so an image in a slot now uses the exact same upload path, with the exact same `courseId`, as a standalone image block. | Verified in the browser, not just by reading the fix: uploaded a real image to the left slot (succeeded, displayed correctly), repeated for the right slot, then moved the two-column block to a different page and uploaded again to confirm move doesn't reintroduce stale context -- all three succeeded. Cross-checked the Media Library panel afterward to confirm no orphaned `"undefined"`-associated assets were created by this session's testing (the pre-fix orphaned-asset failure mode described above was reasoned from code inspection of the old signature, not separately reproduced and left in the database).

2026-07-17 | Table cell superscript/subscript (QA-requested narrow exception to ARCHITECTURE.md 3.7's "cell content is plain text only" rule, for pathology notation like 10³ and del(5q)): chose **HTML tags** (`<sup>`/`<sub>`, sanitized to that two-tag allowlist and nothing else -- no bold/italic/underline/line-breaks in cells) over literal Unicode superscript/subscript characters. Cells reuse the exact same `richText.js` sanitize/render infrastructure built for the formatting-data-loss fix above, just with a restricted `SUP_SUB_TAGS` allowlist instead of the full `RICH_TEXT_TAGS` set, and a matching X²/X₂-only floating toolbar (no B/I/U buttons) in `TableBlockEditor`. | Unicode substitution was the simpler-looking option on its face (no markup, no sanitizer needed, cell content stays a plain string) but was ruled out on a concrete coverage gap, not a stylistic preference: Unicode has superscript digits/some letters but **no subscript letter "q"** at all, which makes it structurally unable to render "del(5q)" -- one of the two examples the QA report itself named as a requirement. The HTML-tag approach has no such gap (any character can go inside `<sub>`/`<sup>`) and cost nothing extra to build since the sanitizer/AST/React-render pipeline already existed for the Issue 1 fix -- reusing it here is strictly less code than building a separate Unicode-mapping table would have been. Verified end to end: typed "10", selected "3", applied superscript via the new toolbar, confirmed "10³" renders correctly in the editor canvas; since the player only ever renders `course.pages[0]` today (a documented, intentional Phase 4 limitation -- see `App.jsx`'s "for now there is only ever one page" comment, out of scope for this task), the table block was temporarily moved to Page 1 to verify player rendering (`<th scope="col">10<sup>3</sup></th>`), then moved back to Page 2 afterward; confirmed the formatting survives a genuine hard reload (fresh navigation from the course library), which also re-validates the Issue 1 fix holding for this new sup/sub case.

2026-07-17 | Pan/zoom for the image lightbox (P1-9), plus the two pieces of infrastructure it turned out to depend on that didn't exist yet: **discovered during Step 1 that neither the unified modal layer (ARCHITECTURE.md 5.3) nor carousel player-rendering (P1-8) had actually been built** -- `ImageBlock.jsx` had no click handler at all, `TextBlock.jsx`'s `onOpenLightbox` prop for term-to-image links was never threaded through `BlockRenderer.jsx` (dead button), and `carousel` blocks rendered a "not available" placeholder in the player (DECISIONS.md 2026-07-12 already flagged this as Phase 5 scope). Built all three in this session, scoped as follows. **Modal** (`packages/player/src/components/Modal.jsx`): a single dialog shell (focus trap, Escape-to-close, close button, WCAG `role="dialog"`/`aria-modal`, restores focus to the trigger element on close) dispatching on `payload.type`. Only the `image` payload got a full build-out (`ImagePayload.jsx`) since it's the only one with real callers today (`ImageBlock`, `CarouselBlock`, `TextBlock` asset-links); `iframe`/`pdf`/`email`/`interactive_video_overlay` payload renderers exist (matching ARCHITECTURE.md 5.3's documented payload list) but are minimal, since PDF.js integration, SMTP/Contact-button UI, and interactive-video pause points are separate, not-yet-built features (P1-18, P1-19, P1-22) -- building those out now would have been Phase 4/5 work smuggled into a zoom-feature task, which the task explicitly said not to do. **Carousel** (`packages/player/src/blocks/CarouselBlock.jsx`, newly registered in `BlockRenderer.jsx`): renders one image at a time with Prev/Next arrows and dot indicators, resolving `content.asset_ids` against the course's shared `assets` array exactly like the existing editor `CarouselBlockEditor.jsx` authors it. **Pan/zoom** (`packages/player/src/lib/imageZoom.js`, a `useImageZoom` hook): CSS `transform: translate()/scale()` only, driven by Pointer Events (unifies mouse-drag and touch-drag/pinch -- two simultaneous pointers switches from pan to pinch-zoom) and `wheel` events (trackpad pinch arrives as `wheel` with `ctrlKey: true`, handled the same code path at higher sensitivity). Zoom range is dynamic, not a flat 100-400%: `computeMaxZoom()` compares the image's `naturalWidth` against its displayed width and caps zoom at roughly the point where the source has no more real pixel detail left to reveal (1 image px = 1 screen px), floored at 150% so small images still allow some zoom and capped at 400% since this is explicitly not the Phase 7 tiled deep-zoom viewer with a real resolution ceiling to chase past that. Zoom resets to 100% on carousel navigation (`ImagePayload`'s zoom-reset effect is keyed on the displayed asset's id, not on mount) and whenever the modal is closed and reopened (payload becomes `null` between opens, so `useImageZoom`'s state is destroyed on unmount and reinitializes at scale 1 -- no explicit reset-on-open code needed). | This is a lightweight, single-resolution CSS-transform zoom, not a tiled/deep-zoom viewer -- there is no canvas, no tile loading, and no support for gigapixel source images; the dynamic max-zoom cap is specifically what keeps it from ever trying to be one (past the point of real detail, it simply won't zoom further, rather than blowing a static image up into visible pixelation). ARCHITECTURE.md 5.4 describes a "shared zoom engine" as Phase 5's compare-block deliverable that Phase 7's deep-zoom viewer extends; `imageZoom.js` is written as a standalone, lightbox-scoped hook rather than a formal shared module (Phase 5's P1-12 compare block, which needs synchronized dual-viewport zoom, doesn't exist yet), but its pan/zoom math (clamped scale, focal-point-aware zoom, pointer/pinch tracking) is deliberately generic and coupled to nothing lightbox-specific, making it the natural seed for that shared engine when Phase 5 actually builds the compare block -- extended then, not replaced. Verified end to end in the browser: standalone image block (click to open, +/- buttons, scroll-wheel zoom, click-drag pan, double-click reset, Reset zoom button, Escape and close-button dismissal with focus returned to the exact button that opened it); a 4-image carousel with captions (opening from a specific image preserves its index, Next/Prev wrap correctly in both directions at the array boundaries, zoom resets to 100% on every navigation, caption and alt text update per-image); keyboard operability (Tab reaches every control, `+`/`-` zoom the focused image, real click-then-Escape confirmed correct focus restoration -- an artifact from an earlier JS-driven `.click()` test that skipped real browser focus semantics was caught and re-verified, not assumed correct); confirmed via the rendered player DOM that alt text is present on the image element regardless of zoom state. Confirmed no regression to the prior session's three fixes (rich-text formatting, two-column slot upload, table sup/sub) by re-reading Page 1/2's stored `course_json` after this session's changes -- all three still intact.

2026-07-17 | **Root cause found and fixed for Media Library multi-select alt-text/caption not persisting**: `CarouselBlockEditor` (`packages/editor/src/components/blocks/CarouselBlock.jsx`) rendered its "Add Images from Library" picker with `onUpdateCourseAsset={() => {}}` -- a hardcoded no-op -- instead of forwarding the real prop, and didn't even destructure `onUpdateCourseAsset` in its own function signature to have it available. `MediaLibraryPanel`'s inline alt-text/caption fields correctly PATCH the DB `assets` table row on blur regardless of this bug (`api.updateAsset`, unaffected), but the field's displayed value -- and what actually gets saved into the course, since the player and every reopen of the Media Library read from `course_json.assets`, not from the DB row directly -- comes from `onUpdateCourseAsset` writing into `course_json` via `CourseEditor.jsx`'s `handleUpdateCourseAsset`. With that callback stubbed to a no-op, the DB row silently got the right value while `course_json.assets` never did, so the edit looked lost on any reopen or reload even though it partially "worked" at the database layer. This only affected the picker reached from a carousel block's "Add Images from Library" button; the standalone toolbar Media Library (`CourseEditor.jsx`'s own direct usage) and the Knowledge Check image pickers both already threaded the real callback through correctly -- confirmed by grepping every `MediaLibraryPanel` call site for the same `() => {}` pattern before concluding this was the only occurrence. Neither hypothesis in the task's own framing was quite right: it wasn't that "the bulk upload handler skips alt-text/caption fields" (the bulk-upload response handling is unrelated code, untouched) and it wasn't a timing race between typing and the async PATCH resolving (waited 7+ seconds past the 5-second autosave debounce during investigation and the value was still missing from the server -- ruling out anything but a permanent, non-timing-dependent drop). Fixed by adding `onUpdateCourseAsset` to `CarouselBlockEditor`'s props and passing it straight through to `MediaLibraryPanel` instead of the stub. | Found by reproducing with real browser interaction end-to-end (not code inspection alone): a first attempt to reproduce via computer-tool clicks appeared to show total data loss, which turned out to be a screenshot-coordinate-scaling mistake in the test itself (clicking outside the actual 800×450 screenshot bounds landed on nothing); redone with `getBoundingClientRect`-derived coordinates, single edits confirmed working fine, isolating the failure specifically to the carousel's selection-mode picker rather than the whole Media Library. Verified end to end: 3-image multi-select upload via "Add Images from Library" with distinct alt-text and captions typed for each, including clicking "Add Selected" immediately after the last field with no separate blur-triggering click first (the most realistic "navigate away right after typing" case) -- confirmed all three persisted correctly via a direct server API read and via a genuine hard reload/re-navigation into the course, not just cached client state.

2026-07-17 | **Root cause found and fixed for the table block not scrolling horizontally at phone width** (QA finding, item 2): `.block-table table` had no explicit width behavior beyond the browser's default table auto-layout, and `.block-table__scroll`'s `overflow-x: auto` wrapper had no `max-width` of its own. A child can only ever trigger its container's `overflow-x` if the child's own rendered width exceeds the container's -- and without a `max-width` cap on the wrapper, the wrapper itself was free to grow to fit the table (rather than the table being forced to overflow it), so the two nearly always ended up the same width and the scrollbar never appeared regardless of column count or content length. Fixed in `packages/player/src/styles/player.css` with two changes working together: `max-width: 100%` on `.block-table__scroll` (caps the wrapper at its parent's width, no longer free to grow with the table) and `min-width: max-content` on `.block-table table` (lets the table size to its actual natural content width, uncapped) -- the mismatch between an unconstrained table and a capped wrapper is what actually makes `overflow-x: auto` do something. Added `min-width: 0` under the existing `@media print` block so this fix doesn't force an awkwardly wide, cropped table onto a printed page, where the previous full-width table behavior is still correct. | Verified with real measurements read from the live DOM, not visual inspection alone: built a 4-column table with long header and cell text (via the item 7 paste-in feature), opened the editor's Phone-width preview, and read `.block-table__scroll`'s computed layout inside the preview iframe -- `clientWidth` 357px vs `scrollWidth` 980px (confirming the table's natural width now genuinely exceeds its wrapper) with `overflow-x: auto` confirmed via computed style. Confirmed the fix contains the overflow correctly rather than just moving the bug up a level: the surrounding page `<body>` stayed at 373px `scrollWidth` against a 357px `clientWidth` (a few pixels of default margin, not a broken layout) -- only the table's own wrapper absorbs the excess width, the page itself never overflows the viewport.

2026-07-17 | **Root cause found and fixed for the embed block causing preview to jump-scroll to its position on load** (QA finding, item 3): matches suggested-cause #3 from the QA report -- this is standard browser default behavior where a newly-loaded iframe, once its own document finishes loading, can receive focus, and some browsers resolve that focus shift by auto-scrolling the parent document to bring the newly-focused element into view, even though nothing in the course content asked for that. Ruled out the other two candidate causes named in the report before concluding this: no `autofocus` attribute is set anywhere on the embed iframe or its wrapper, and `DEFAULT_EMBED_SANDBOX` (`packages/editor/src/lib/blockDefaults.js`) was already correctly scoped -- it omits `allow-top-navigation`, which is the specific sandbox permission that would let embedded content control the parent page's scroll/URL, so an overly permissive sandbox wasn't the mechanism. Fixed defensively in `packages/player/src/App.jsx` with a `useEffect` that calls `window.scrollTo(0, 0)` once on mount, then again on every `.block-embed__iframe`'s native `load` event, with the event listeners removed on unmount. A mount-only reset isn't sufficient by itself: the `load` event is the actual moment a browser's default focus-shift can occur, and it happens well after mount since the embedded remote content takes real network time to load. | Verified live in the browser: added an embed block (pointed at `https://example.com`) below a paragraph of text and a wide table on a page tall enough to have real scroll height, opened the Phone-width preview, and read `window.scrollY` from inside the preview iframe both immediately on open and again after a 2-second wait for the embed's own network load to settle -- 0 both times, with the page visibly showing its top ("Page 1" heading) rather than jumped to the embed's position further down. Cross-origin restrictions on the embedded `example.com` document itself prevented directly confirming *that specific* iframe fired its `load` event (its `contentDocument` is inaccessible from the parent, which is correct, expected browser behavior for a cross-origin frame, not a bug) -- so this confirms the fix's mechanism holds scroll position at 0 through the full preview-open sequence, rather than freshly reproducing the pre-fix browser auto-scroll behavior itself inside this automated test environment.

2026-07-17 | **Accordion/tabs nested-block container migration approach (item 6): no schema, player, or data migration was needed** -- the data model this task's own framing anticipated needing to build already existed everywhere except the editor's authoring UI. Investigated before writing any migration code: `packages/schema/course.schema.json` leaves every block's `content` field fully unconstrained (`{"type": "object"}`, no per-block-type schema beyond `two_column`'s `left`/`right` slots), so no schema change was possible or needed. `packages/player/src/blocks/AccordionBlock.jsx` and `TabsBlock.jsx` already rendered `(item.body_blocks || []).map(childBlock => <BlockRenderer .../>)` for each item/tab -- the player-side container-rendering ARCHITECTURE.md 3.6 describes for two_column was already in place for accordion/tabs too, just with no editor UI able to produce more than one inner block. `samples/sample-course.json`'s accordion/tabs blocks already used real `body_blocks` arrays with proper nested block objects (`blk_acc1_text1`, `blk_tabs1_gross_text`), not a legacy single-`body` field -- confirmed by direct inspection, not assumption. Queried every course in the dev database (`curl` against each `/api/courses/:id`) and parsed each accordion/tabs item's keys in Python -- zero uses of a legacy `body` field anywhere across all 8 existing courses; every one already used `['title'/'label', 'body_blocks']` exclusively. `CourseEditor.jsx`'s `assignBlockIds`/`rebuildBlockWithIds`/`regenerateIds` (the id-remapping functions undo/redo, duplicate, and save all depend on) already recursed into `item.body_blocks` generically, not just two_column's `left`/`right` -- confirmed by reading the actual recursion code, not inferred. **Net effect: this item was scoped down to a pure editor-UI build** -- a new shared `ItemBlockStack.jsx` component (used by both `AccordionBlock.jsx` and `TabsBlock.jsx` editors) that renders each item's `body_blocks` as a stack with up/down/remove controls per block and an "Add Text / Add Heading / Add Image" picker at the bottom, mirroring `TwoColumnBlock.jsx`'s slot pattern -- reusing `createInnerBlock()` from `blockDefaults.js` with a generalized suffix (`item${index}_blk${n}`) instead of two_column's fixed `left`/`right`, producing ids like `blk_acc1_item0_blk1` matching the exact convention two_column already established. No existing course content needed migration or manual recreation as a result -- there was nothing in the old shape left to migrate. (Item 5's accordion/tabs rich-text-toolbar requirement was folded into this same change rather than built separately against the old single-field structure: the new stack's text blocks are real `TextBlock.jsx` instances, which already carry the full B/I/U/Sup/Sub/color toolbar, so building a toolbar for the soon-to-be-replaced old field would have been immediately-discarded work.) | Verified end to end in the browser, not just by reading the code: added an accordion block, added a text block and then an image block inside one item via the new stack picker, typed real text into the text block, saved, and read the persisted `course_json` back from the server API -- `body_blocks` contained exactly `[{type: "text", block_id: "blk_e7c2yn_item0_blk0", ...}, {type: "image", block_id: "blk_e7c2yn_item0_blk1", ...}]`, confirming both the multi-block stack and the namespaced id convention work as intended without any of the anticipated migration machinery.

2026-07-17 | **Curated text color palette values and their measured contrast ratios (item 8)**: `{name: 'Default', value: null}`, `Primary Blue #2563EB` (unmodified `--mn-color-primary-blue`), `Violet #6D28D9` (unmodified `--mn-color-purple` -- chosen over the brighter `--mn-color-violet` `#7C3AED` for better contrast: 7.10:1 vs 5.70:1 against white), `Emerald #127D59` (darkened from the raw brand token `--mn-color-emerald` `#10B981`, which measures only 2.54:1 against white -- nowhere near AA), `Coral #A82424` (darkened from the raw brand token `--mn-color-coral` `#FF6B6B`, which measures only 2.78:1 against white), `Deep Navy #0A1020` (unmodified `--mn-color-deep-navy`). Measured contrast ratios (WCAG relative-luminance formula, computed programmatically, not eyeballed) against the light default background (`#FFFFFF`, the app's `--color-surface`): Primary Blue 5.17:1, Violet 7.10:1, Emerald 5.12:1, Coral 7.14:1, Deep Navy 18.96:1 -- all comfortably clear the 4.5:1 AA threshold. Against a dark-navy background (`#0A1020`, the value of `--mn-color-deep-navy`, used as the stand-in for "inside the player top bar context" since no such dark-background text context actually exists in the app today): Primary Blue 3.67:1, Violet 2.67:1, Emerald 3.70:1, Coral 2.66:1 -- **none of the five clear 4.5:1**, and Deep Navy itself is inapplicable against a navy background by definition (a color used as its own background renders invisible against itself, the same trivial degenerate case as white-on-white). **This is not an oversight left unaddressed: it's a mathematical impossibility for these two specific background extremes, verified by brute-force search, not just asserted.** A color's relative luminance would need to fall in the range [0.183, 0.199] to clear 4.5:1 against both `#FFFFFF` (luminance 1.0) and `#0A1020` (luminance ≈0.0054) simultaneously -- computing the two 4.5:1 boundary conditions algebraically gives a required lower bound (0.199) that is numerically *greater* than the required upper bound (0.183), an empty, inverted range with no solution regardless of hue or saturation. Confirmed computationally: searched hue 90°-175° (the green-toward-teal range closest to threading this needle, since Emerald's darkened value came closest of all five candidates at 5.12:1/3.70:1) across saturation 0.5-1.0 and lightness 0.25-0.34 in fine steps -- zero colors found passing both thresholds simultaneously. Resolution: since headings/text blocks never actually render against a dark-navy background anywhere in the current app (confirmed by reading `player.css` -- `.player__course-header`, the only candidate container, uses the standard light `--color-text-secondary` on the standard light background, no dark variant exists), the light-background 4.5:1 requirement was treated as the hard, real-usage gate (all five colors clear it comfortably), and each color's dark-navy contrast was chosen to be as high as achievable while still comfortably clearing the light-background threshold, rather than silently ignored. If a dark-background text context is ever added in a later phase, it will need its own separate palette (a single static hex value baked into stored content HTML via `document.execCommand('foreColor', ...)`, per this task's specified implementation approach, cannot be background-context-adaptive) -- flagged here for that future work, not fixed now. | Also discovered and fixed during implementation, not a pre-existing issue: `document.execCommand('foreColor', ...)` with `styleWithCSS` enabled attaches the `color` style directly onto whatever inline element the selection sits fully inside (e.g. `<b style="color:...">`) rather than always wrapping a fresh `<span>` -- confirmed by hand via direct DOM inspection, not assumed from documentation. The sanitizer (`packages/{editor,player}/src/lib/richText.js`) originally only checked for a color on `SPAN` tags specifically, silently dropping color applied to already-bold (or otherwise already-formatted) text. Fixed by moving the color check to run against every element node before its tag-specific handling, wrapping whichever children carried a valid palette color in a `{type: 'span', color}` AST node regardless of which literal tag carried the style. Verified in the browser: selected the word "paragraph" inside existing bold text, applied Violet via the new toolbar, confirmed the live DOM showed `<b style="color: rgb(109, 40, 217)">paragraph</b>`, then blurred the field and confirmed it canonicalized to `<b><span style="color:#6d28d9">paragraph</span></b>` (both bold and color preserved together) -- then reloaded the course fresh and opened the player preview, confirming the same text rendered visibly bold and violet through the full save/reload/player-render pipeline, not just inside the editor's own DOM.

2026-07-17 | **Real root cause of the table-not-scrolling regression (QA item A): not a broken containment mechanism, and not a divergent editor-preview-vs-player rendering path -- a missing scrollbar-visibility affordance.** Investigated both hypotheses the report raised and ruled both out with direct evidence before finding the real cause. (1) Divergent rendering paths: confirmed the editor's own live preview panel (`CourseEditor.jsx`'s `previewMode` iframe, `src="/player?courseId=...&preview=true"`) is proxied by `packages/editor/vite.config.js` straight to the content-server's `/player` route -- the exact same built player bundle the standalone player uses, not a second implementation. Reproduced the EXACT failing scenario (a table created via the item-7 paste-in feature, pasted with realistic multi-sentence clinical text copied the way a real Word table would be, viewed at the editor's own "Desktop" preview toggle with the settings panel still visible) and confirmed the table visibly cut off at the right edge with no scrollbar in BOTH the editor's preview iframe and the standalone player at the same width -- ruling out a divergent path, since a genuinely separate implementation wouldn't fail identically in both. (2) Paste-in inline-style conflict: inspected the live DOM of every pasted cell (`<th>`/`<td>` in the rendered player output, and the editor's own contentEditable cells) after a real paste -- zero inline `style` attributes, zero `width` attributes, `table-layout: auto` -- the item-7 paste handler (`packages/editor/src/components/blocks/TableBlock.jsx`) only ever writes plain sanitized text into `rows[r][c]`, never touches cell styling. Ruled out. **The actual cause:** confirmed via `scrollLeft` manipulation that `.block-table__scroll` genuinely IS a working `overflow-x: auto` container (setting `scrollLeft = 200` visibly shifted the table's content) -- the mechanism from the prior session's fix (`min-width: max-content` on the table, `max-width: 100%` on the wrapper) is correct and functioning. What's missing is any VISIBLE indication of that: the wrapper painted zero scrollbar in this test environment, and does so under real desktop macOS/iOS-style "overlay scrollbar" browser settings too -- a table that is functionally scrollable but shows no scrollbar looks, to a human, indistinguishable from a table that's simply broken and cut off. Fixed two ways in `packages/player/src/styles/player.css` and `packages/player/src/blocks/TableBlock.jsx`: explicit `::-webkit-scrollbar`/`scrollbar-width`/`scrollbar-color` styling on `.block-table__scroll` (the standard technique for opting an element out of OS overlay-scrollbar behavior into a persistent classic scrollbar in real Chromium/Safari/Firefox), plus a JS-measured fallback that doesn't depend on scrollbar rendering at all: `TableBlock.jsx` now measures `scrollWidth > clientWidth` on mount and on resize and renders a small "↔ Scroll to see more" hint below the table only when it's genuinely overflowing. | Verified live in both required contexts per the QA report's own reproduction steps: pasted a realistic 3-row x 4-column clinical table via the paste-in feature, confirmed the "↔ Scroll to see more" hint renders in the editor's own Desktop preview panel (settings panel still visible) AND at Phone width, AND in the standalone player at 1280px -- all three previously showed the identical cut-off-with-no-affordance failure and all three now show the hint. Could not fully confirm real-desktop-Chrome classic-scrollbar rendering from within this sandboxed test browser (`offsetHeight - clientHeight` stayed 0 even after applying the `::-webkit-scrollbar` styling, meaning this specific automated browser context doesn't switch out of overlay-scrollbar mode the way real desktop Chrome/Safari are documented to) -- flagged explicitly rather than asserted as confirmed; the JS-measured hint is what's actually verified end-to-end and does not depend on that browser behavior at all.

2026-07-17 | **Real root cause of the embed scroll-jump regression (QA item B): confirmed the delayed-async-focus hypothesis, and separately discovered the fix's first draft (a `focusin` listener) would have silently never fired for a genuinely cross-origin embed -- the exact case it exists for.** The prior session's fix only corrected scroll at mount and at each embed iframe's own `load` event, assuming any browser-driven focus-steal happens right at load; QA's regression is consistent with a real WSI/deep-zoom viewer, which loads its shell fast (firing `load` immediately) then spends real time afterward asynchronously fetching and rendering tiled image content, commonly calling `.focus()` on its own canvas once that finishes so keyboard pan/zoom works immediately -- seconds after `load`, well after the original fix had stopped watching. First redesign attempt used a `focusin` listener on the embed iframe element instead of (only) `load`, reasoning that `focusin` would catch a focus grab at any time regardless of cause. Built it, then **verified it directly rather than trusting the reasoning**: dispatched a real `.focus()` call on a test iframe (an opaque-origin `data:` URL, which triggers the same cross-origin-style restrictions a real DigitalScope embed would) and confirmed `document.activeElement` correctly updates to the iframe, but the `focus`/`focusin` EVENT never fires in the parent document at all -- this is a deliberate, documented browser privacy behavior (suppressing cross-origin focus-timing events so a parent page can't fingerprint activity inside content it doesn't control), not a bug in the test. This meant the `focusin`-based fix would have looked correct in code review and would have silently done nothing for a real cross-origin WSI embed, the exact scenario the fix exists for. Redesigned to poll `document.activeElement` on a 200ms interval instead (confirmed reliable regardless of origin), triggering a scroll correction only when the newly-active element is a tracked embed iframe. Two further refinements, both to avoid a new failure mode of "fights the learner": (1) a real click on the embed (a `pointerdown` on the iframe immediately before it gains focus, observable in the parent document even cross-origin, since the click lands on the outer `<iframe>` element before focus transfers into its content) is never corrected -- that's the learner deliberately using the embed, not an unsolicited focus-steal; (2) once the learner has interacted with the page at all (wheel, touch, keydown, or a real click anywhere), corrections stop permanently for that page view -- the bug is specifically about the page's initial top-of-load state being hijacked before the learner does anything themselves, not about pinning scroll to the top forever, which would fight a learner legitimately scrolling down later. | Verified each mechanism directly, not just by re-running the original "check scrollY" test that passed last time despite the bug persisting: (1) unsolicited focus grab (iframe.focus() with no preceding pointerdown, after a genuine blur-to-body transition so the poll had a real state change to detect) -- scroll position correctly reverted; (2) deliberate click (pointerdown dispatched on the iframe immediately before focus) -- scroll position correctly left alone; (3) post-interaction focus grab (a wheel event dispatched first, then an unsolicited focus grab) -- scroll position correctly left alone, confirming the fix does not fight a learner who has started using the page. Could not reproduce the underlying native browser auto-scroll-on-focus behavior itself in this sandboxed test environment end to end (same limitation as the original 2026-07-17 embed fix entry) -- monitoring `window.scrollY` continuously for 19+ seconds against a simulated delayed-focus WSI test page never showed a jump, with or without either fix in place, meaning this specific automated browser context does not appear to exhibit the native auto-scroll-on-focus quirk the way QA's real desktop browser does. What IS verified end-to-end is that the correction mechanism itself is correctly wired, fires precisely (and only) when it should, and does not fight legitimate learner interaction -- which is the part actually within this codebase's control.

2026-07-17 | **Why the prior session's verification passed for both items despite QA reproducing both bugs again -- a testing-methodology gap, documented so it doesn't repeat.** Table item: the prior verification read `scrollWidth`, `clientWidth`, and computed `overflow-x` programmatically and concluded "scrollable" -- which was TRUE and remains true; the mechanism was never actually broken. But that check never asked the one question a human tester asks: is there anything on screen indicating this can be scrolled at all? A `scrollWidth > clientWidth` assertion proves the CSS mechanism works; it says nothing about whether a scrollbar is visually present, which is a separate, browser/OS-dependent rendering behavior (macOS/iOS overlay scrollbars specifically) that no computed-style check can catch -- only an actual screenshot or a real scrollbar-hover-and-look can. The verification also used short placeholder cell text ("Substantial Column Header One") rather than reproducing the paste-in feature specifically, and tested only at Phone width, never at the Desktop preview toggle QA's report called out specifically -- neither gap was the actual cause here (the bug reproduced identically at every width and via manual typing too, once looked for), but both are exactly the kind of narrowing that lets a regression hide from a test that technically exercises "the same feature." Embed item: the prior verification checked `window.scrollY` at the iframe's `load` event and shortly after, using a simple embed URL that has no asynchronous post-load behavior -- it never tested a case where focus-stealing content finishes loading well after `load` fires, which is exactly the gap a real WSI/deep-zoom viewer falls into. Checking a signal "at the moment it's expected to fire" instead of "for as long as the underlying condition could still occur" is the same shape of gap in both items: a check that is correct for the specific artifact tested, but scoped narrower than the real-world case a learner or author will actually hit. Going forward: verifying a fix for "renders correctly" or "scroll behaves correctly" needs an actual rendered screenshot or a continuously-monitored signal over a realistic time window, not only a single computed-style or single-timestamp assertion, and needs to be run against the specific reproduction path named in the bug report (the exact feature, the exact viewport, the exact content shape) rather than a simplified stand-in for it.

2026-07-18 | Phase 4 Part 1: player chrome (top bar, progress bar, nav drawer, Continue button with gating, utility bar). Added `meta.nav_mode`/`page_display`/`page_groups`/`utility_bar` and page-level `triggers`/`continue_gate` to `course.schema.json`; `samples/sample-course.json` restructured from one page into three pages across two groups (`nav_mode: linear`, `page_display: grouped`) to actually exercise grouping/locking/multi-page Continue, rather than leaving those features schema-valid but untestable against a single-page sample. Deliberately did NOT add `page.completion_status` (runtime-only, tracked via `completedPageIds` state + SCORM `suspend_data`, per this task's explicit instruction) and did NOT build any author-facing UI for setting `continue_gate`/`triggers` (Part 2 scope, see gating entry below). | Standalone player (`localhost:3001/player`) verified end to end across all 11 integration-test points: top bar, 0%-start progress bar, grouped drawer with correct status icons and disabled locked pages, Continue advancing progress/drawer state, locked-page click no-op, Resources/Contact modals, 375px hamburger-overlay + bottom utility bar, `nav_mode:"free"` unlocking previously-locked pages, and no regression across every existing block type (text/heading/image/list/accordion/tabs/knowledge-check/table/two-column/embed/carousel/reflection). Editor-preview context was **not** directly verified live: `content-server` (port 3001) crashed within seconds of every restart across six separate attempts this session, independent of any restructuring of the server-restart sequence tried (restarting content-server alone, restarting both content-server and editor-dev together, restarting in either order) -- confirmed via direct reading of `packages/server/src/index.js` that this is not caused by the schema/sample changes in this task (the server performs no schema validation or sample-course loading at startup). Relying instead on architectural equivalence, confirmed by code inspection: `CourseEditor.jsx`'s preview iframe loads the identical `/player?courseId=...&preview=true` route off the identical content-server bundle the standalone player uses (same fact already established and used for two prior QA-regression entries above, 2026-07-17 items A/B), and `isPreview` in `App.jsx` is threaded only into `BlockRenderer` (governing an unrelated, pre-existing embed-block behavior) -- none of the five new chrome components receive or branch on it, so there is no code path where chrome could render differently between the two contexts. Flagged here as an unverified-live gap for a follow-up manual check once the content-server instability is resolved, not asserted as a confirmed pass.
2026-07-18 | Contact utility-bar item uses `mailto:` as the primary action, with an always-visible "Copy email address" button alongside it (not a fallback that only appears if `mailto:` fails) -- see `EmailPayload.jsx`'s own header comment for the full mechanism. Chose mailto-primary over building a copy-only or clipboard-detection-gated UI because the launcher's own iframe (`packages/launcher/template/index.html`) carries no `sandbox` attribute, so `mailto:` is not blocked in the one context (SCORM launch) where an unexpected sandbox restriction was actually a real risk to check for -- confirmed by reading the launcher template directly, not assumed. | Both actions are offered together, permanently, rather than picking one: `mailto:` silently does nothing (no error, no visible failure) on a device with no mail client configured, which is common on shared/lab/hospital computers this course is likely to run on, and the player has no way to detect that failure after the fact to fall back automatically -- so "always show both" is more reliable than "try one, fall back to the other" for a failure mode that's invisible to the code. Clipboard copy itself is wrapped in try/catch (confirmed via live testing to throw `NotAllowedError` in this session's sandboxed browser-automation context specifically -- expected, and not a bug, since a real browser session with a genuine user gesture succeeds) so a clipboard-permission failure degrades to "button visibly does nothing new" rather than a crash.
2026-07-18 | Continue-button gating mechanism: added `continue_gate` (a single `{all:[...]}/{any:[...]}` condition object, reusing the exact structure Phase 1's `evaluateCondition` in `triggerEngine.js` already implements) to the **page** schema definition, not the course level. `App.jsx` computes `continueDisabled = page.continue_gate ? !evaluateCondition(page.continue_gate, variables) : false` on every render and disables the Continue button (native `disabled`, no click handler fires) whenever it evaluates false; a page with no `continue_gate` is always ungated, matching every page in the current sample course. Page-level `triggers` (an array, same shape as Phase 1's existing trigger objects) was also added so `onPageEnter`/`onPageExit` have somewhere to fire from -- `App.jsx` runs `runTriggers(variables, page.triggers, 'onPageEnter')` in a `useEffect` keyed on `currentPageId` (including the initial page, not just subsequent navigations) and fires `onPageExit` triggers just before every navigation away from a page (drawer click, Continue click, and utility-bar jump-to-page all funnel through the same exit-then-navigate sequence). No author-facing UI to write either field was built -- both are read-only from the player's perspective, per this task's explicit Part-2-scope boundary. **What Part 2's trigger builder needs to write into the JSON to use this:** a `condition` object (the existing `{all/any: [{var, op, value}, ...]}` shape, already schema-defined and already consumed by Phase 1's trigger `WHEN` clauses) assigned directly to a page's `continue_gate` field -- no new condition syntax, no new evaluation code, and no player-side changes required; Part 2 only needs an authoring surface (e.g. "gate Continue on..." control in a page's settings) that serializes to this one field, plus, if it wants onPageEnter/onPageExit authoring, a `triggers` array on the same page object using the same trigger shape already used for block-level `onOpen`/`onCorrect`/etc. triggers. | Page-level (not course-level) because gating conditions are inherently about a specific page's content (e.g. "answered the knowledge check on this page correctly") -- a single course-level gate couldn't express "page 3 requires the page-2 quiz" without also inventing some page-scoping wrapper around it, which is just a page-level field by another name. Free mode does not bypass `continue_gate` -- gating and nav-mode are orthogonal: `nav_mode` controls whether the *drawer* allows jumping around, `continue_gate` controls whether *this specific page's* Continue button is enabled, and both apply identically regardless of the other's setting, matching this task's explicit "free mode: Continue still works identically" instruction.
2026-07-18 | Top bar layout: the logo was **omitted entirely** from `TopBar.jsx`, not shrunk or conditionally shown -- only the hamburger and truncated course title render, matching the task's own stated fallback ("skip the logo, prioritize title + hamburger" if it doesn't fit). At the 56px `--player-chrome-height` bar height, fitting a legible logo lockup, a hamburger, a course title with real room to breathe before truncating, and (on desktop) the utility links all in one row left no width budget that didn't force the title into truncating almost immediately on any moderately long course name -- the same title-overflow failure mode already fixed once in the editor's own top bar (2026-07-16 entry above) and explicitly called out in this task as a pattern to avoid repeating. Course identity (title) and primary navigation (hamburger) were judged more load-bearing for a learner mid-course than brand presence, which is already established once at course launch (course library / SCORM launch screen) outside the player chrome itself. | No partial/icon-only logo variant was attempted as a compromise -- a bare mark with no wordmark at 56px height was judged to read as visual noise next to the title rather than a meaningful brand cue, and this task's own instructions treated title+hamburger as an acceptable, fully-formed fallback rather than something to work around with a smaller asset.

2026-07-18 | Phase 4 Part 2: variable manager, trigger builder UI, continue_gate authoring UI. No schema changes were needed at all -- `course.variables`, `block.triggers`/`block.visibility`, `page.triggers`/`page.continue_gate`, and the full trigger/condition/action vocabulary were already present in `course.schema.json` from Phase 1 and Part 1, ahead of any authoring UI (same "schema hooks added ahead of their UI" pattern as Phase 3.5). New editor components: `VariableManagerPanel.jsx` (Variables tab), `PageSettingsPanel.jsx` (Page tab: Continue gate + page-level triggers), `TriggerBuilderModal.jsx` + `ConditionBuilder.jsx` (the shared sentence-style builder, reused for both a trigger's optional "if" and a page's Continue gate), `TriggersSection.jsx` (per-block collapsed Triggers list), and `triggerUtils.js` (event/action tables, plain-English sentence rendering, variable-usage counting for the delete warning). `SettingsPanel.jsx`'s previously single-purpose right panel became a three-tab switcher (Course / Page / Variables) when no block is selected, to give Page Settings and Variables their own home without crowding Course Settings. A real, pre-existing bug in `triggerEngine.js` was found and fixed while building this: `runTriggers` filtered every trigger's condition against the *original* variables in one pass, before any trigger's own actions ran, so two triggers on the same event (one setting a variable, a second reading it) couldn't see each other's effects within the same firing -- exactly the pattern Step 8's own integration test exercises. Fixed by checking each trigger's condition immediately before running its actions, in a single ordered pass, not two. | Verified end to end through the editor UI only, no manual JSON editing, per Step 8: created a course, a Boolean variable, an accordion with two triggers on the same `onOpen` event (one `SET_VAR`, one conditional `SHOW_BLOCK` reading the var the first trigger just set), a text block hidden by default, and a Continue gate reading the same variable -- confirmed in the live preview that the text stays hidden and Continue stays disabled until the accordion is opened, at which point both flip correctly in one action. Also confirmed deleting a variable used in 3 places (2 triggers + 1 continue_gate) warns first, and that undo restores a deleted variable as one discrete step. One caveat: the player bundle must be rebuilt (`npm run build --workspace=packages/player`) before source changes appear in the editor's live-preview iframe or the standalone player -- same pre-existing constraint noted in Part 1, re-confirmed here since it was hit again mid-session (the hidden-block and dual-trigger behavior both appeared broken in preview until the bundle was rebuilt, before any code was actually wrong).
2026-07-18 | AND/OR condition mixing (Step 4): constrained to a single logical operator per condition -- every row in a trigger's "if" or a page's Continue gate shares one AND-across-all-rows or one OR-across-all-rows, never both in the same condition, and never nested (`ConditionBuilder.jsx`'s `conditionToRows`/`rowsToCondition`). This is a deliberate v1 simplification, not a schema limitation: `course.schema.json`'s `condition` definition already supports arbitrarily nested `{all/any: [...]}` trees, and `evaluateCondition` in `triggerEngine.js` already evaluates them recursively -- a future session can add nested-group authoring UI without touching the schema or the engine, only `ConditionBuilder.jsx`. A single condition with exactly one row round-trips to the schema's bare-comparison form (no `all`/`any` wrapper) rather than always wrapping in `{all: [...]}`, keeping simple, single-condition triggers (the large majority, based on the sample data built while testing this) as plain, minimal JSON. | A segmented AND/OR toggle appears only once 2+ rows exist and applies to the whole condition -- this keeps the UI honest about what it can actually produce (flat, single-operator groups) rather than implying per-pair operator control it doesn't have. Full nested logic ("(A and B) or C") was judged not worth the added UI complexity until an author actually needs it; the flat model covers every condition this session's own testing needed to build, including the Step 8 test's two-condition case (readIntro comparisons never needed combining with anything else).
2026-07-18 | Continue-gate authoring UI (Step 5) writes directly into `page.continue_gate` as a bare comparison or `{all/any: [...]}` condition object -- confirmed against Part 1's own 2026-07-18 DECISIONS.md entry ("a single `{all:[...]}/{any:[...]}` condition object... reusing the exact structure Phase 1's `evaluateCondition`... already implements") before writing a single line of authoring code, and the shapes match exactly with no adapter or translation layer needed. UI: a "Page" tab (`PageSettingsPanel.jsx`) with a "Require a condition before Continue is enabled" checkbox that reveals the same `ConditionBuilder.jsx` used inside the trigger builder's optional "if" -- toggling it on seeds one empty condition row; toggling off removes the `continue_gate` key from the page object entirely (not an empty/null placeholder), matching how `App.jsx`'s `continueDisabled` check already treats a missing `continue_gate` as always-ungated. This is not implemented as a `CONTINUE_GATE` trigger action (the task's own alternative suggestion) -- there is no such action in `course.schema.json`'s action enum, and adding one would have meant either duplicating the condition-authoring UI inside a trigger action row for no behavioral gain, or building a second way to express the same single condition object Part 1 already reads directly off the page. The simpler page-settings-checkbox form was chosen instead, exactly as the task flagged as an acceptable alternative when the event/condition/action sentence pattern doesn't fit cleanly (a page-wide gate isn't tied to any one event the way a block trigger is). | Zero player-side changes were required to support this -- `App.jsx`'s `continueDisabled = page.continue_gate ? !evaluateCondition(page.continue_gate, variables) : false` (built in Part 1) reads whatever `page.continue_gate` contains with no knowledge of how it got there, so an author-set condition and a hand-written test fixture are handled identically. Verified live: setting the gate to "readIntro is true" via the UI produced a visibly disabled Continue button that enabled itself the instant the accordion's `SET_VAR` trigger fired, with no page reload or extra step.
2026-07-18 | Actions/events with schema support but no Part 2 authoring UI, for Phase 5 to know what remains: **Actions** -- `ENABLE_BLOCK`/`DISABLE_BLOCK` (schema-defined alongside `SHOW_BLOCK`/`HIDE_BLOCK` but no author-facing use case exists yet: nothing in the player currently reads a block "enabled/disabled" runtime state the way it reads visibility), `OPEN_MODAL` (skipped per the task's own instruction -- image lightbox already opens automatically on click, and no other in-player modal content type has an author-configurable trigger path yet), `SET_STATE` (block "states," REQUIREMENTS.md P1-3, are themselves unbuilt), `SCORM_COMPLETE`/`SCORM_SET_SCORE` (no scoring-authoring UI exists; SCORM completion/score are still computed automatically from `completion_rule` + knowledge-check answers, not author-triggerable). **Events** -- `onTimerEnd` (schema-defined, no timer/countdown block exists to fire it), `onVarChange` (schema-defined; no author-facing use case identified yet since every variable change already happens via an explicit trigger the author authored, so "notify me when any variable changes" has no obvious use this session's testing surfaced), and every video/audio/interactive-video event (`onPlay`, `onPause`, media `onComplete`, `onTimeReached`) plus `button`'s `onClick`, all skipped because those block types don't exist in this codebase yet (Phase 5, REQUIREMENTS.md P1-2/P1-22) -- same "don't build UI for events on block types that don't exist yet" instruction Part 1 followed for player chrome. `onModalOpen`/`onModalClose` (ARCHITECTURE.md 4) aren't even in `course.schema.json`'s trigger event enum yet, so they weren't a Part 2 authoring gap so much as a still-open schema gap, noted here for whichever phase adds modal-driven triggers. | Every one of these already has its schema surface (or, for the modal events, an easy schema addition) reserved, so none of this requires a schema migration when its turn comes -- only new entries in `triggerUtils.js`'s `EVENTS_BY_BLOCK_TYPE`/`ACTION_TYPES` tables and, for actions, a new `ActionRow` case in `TriggerBuilderModal.jsx`, plus (for ENABLE_BLOCK/DISABLE_BLOCK and SET_STATE) real engine-side effect handling in `triggerEngine.js` alongside the SHOW_BLOCK/HIDE_BLOCK effects added this session.

2026-07-18 | Phase 4 Part 3: minimal video/audio blocks + media manager (one-active-media, container-aware pause/resume, audio scroll-pause), closing out Phase 4. Schema change was a single line: `asset.kind` enum extended from `["image"]` to `["image","video","audio"]` -- no dedicated `video`/`audio` block-content schema was added, matching the existing precedent that simple field-shape blocks (carousel, reflection, embed) rely on `block.content`'s already-unconstrained `{"type":"object"}` rather than a bespoke schema, reserved for blocks with real structural constraints to enforce (two_column's split bounds, table's row-consistency). `packages/server/src/routes/assets.js`'s single-file upload endpoint now detects `kind` from MIME type (`video/mp4`/`video/webm`, `audio/mpeg`/`audio/wav`/`audio/mp4`) instead of hardcoding `'image'`, with a per-kind size ceiling (images 10MB unchanged, audio 50MB, video 200MB) enforced after upload rather than via multer's single global limit, so an oversized image doesn't need to share a 200MB ceiling and a legitimate video isn't rejected by a 10MB one. `MediaLibraryPanel.jsx` (image-only UI: bulk upload, `<img>` thumbnail grid, carousel/KC-image pickers) now filters its asset list to `kind === 'image'` defensively, since video/audio assets now land in the same DB `assets` table via VideoBlock/AudioBlock's own dedicated upload zones and would otherwise render as broken `<img>` tags if the library panel were ever opened. New player modules: `engine/mediaManager.js` (module-level singleton -- one active media item across the whole page, across block types) and `blocks/useMediaBlock.js` (shared registration/position-memory/event-wiring hook used by both `VideoBlock.jsx` and `AudioBlock.jsx`, which otherwise differ only in the native element tag and AudioBlock's added IntersectionObserver scroll-pause). `EVENTS_BY_BLOCK_TYPE` in `triggerUtils.js` gained `video: ['onComplete']` / `audio: ['onComplete']`; since `onComplete` already existed for knowledge-check with a different plain-English meaning ("an answer is submitted"), the flat `EVENT_LABELS` lookup was replaced with a new `eventLabelFor(event, blockType)` function that branches only for this one ambiguous event, threaded through `TriggerBuilderModal.jsx`'s event dropdown and `describeTrigger`'s sentence rendering via a new `blockType` context field. | Three apparent bugs surfaced during testing this session and were run to ground before being ruled out as real issues, recorded here so a future session doesn't re-chase the same false leads: (1) the hidden hidden-until-shown text block and the dual-trigger accordion pattern from Part 2 appeared broken in the editor's live preview -- root cause was the player bundle not yet being rebuilt after source changes, the exact same pre-existing constraint documented in Parts 1 and 2 of this phase, re-triggered because Part 3's testing crossed a rebuild boundary again; (2) a real trigger-engine ordering bug WAS found and fixed while re-verifying this (see below); (3) an audio scroll-pause check that read `audio.paused` only 300-400ms after a programmatic scroll returned `false` -- increasing the wait to ~1s showed it pausing correctly, meaning `IntersectionObserver` callbacks in this specific browser-automation environment fire on a longer latency than a real user's browser typically does, not a product defect (confirmed by re-testing with a genuinely generous wait, and by the fact the callback DOES fire, just later than a tight polling window assumed); (4) a `SHOW_BLOCK` effect from a video's `onComplete` trigger appeared to have no visible effect on first check -- adding a temporary `console.log` to `App.jsx`'s `applyEffects` confirmed the effect data was correct and the state update fired, and a fresh DOM query immediately after (rather than the original check, which had queried across a stale page state from an earlier scroll test) found the revealed block present -- a test-sequencing artifact in this session's own script, not a bug in the shipped code (the temporary log was removed before commit, verified via grep).
2026-07-18 | iOS-safe autoplay (Step 2/ARCHITECTURE.md 6): implemented the simplified, platform-agnostic approach ARCHITECTURE.md's own text flags as acceptable -- when `content.autoplay` is true, the `<video>`/`<audio>` element always starts `muted` regardless of detected platform, with a visible "🔇 Unmute" button overlay that unmutes on a real click (`useMediaBlock.js`'s `unmute()`, setting both React state for the button's visibility and `mediaRef.current.muted = false` directly on the element). No iOS/Safari user-agent sniffing was attempted. | Real platform detection (UA sniffing, or the `navigator.userAgentData` API where available) is inherently fragile -- user agent strings are spoofable, change across iOS/Safari versions, and the underlying browser policy this is working around (iOS blocking unmuted autoplay) is itself just one instance of a broader, increasingly common autoplay-restriction policy that other mobile browsers and even some desktop browsers under battery-saver or "reduce data" modes also enforce inconsistently. Always starting muted-with-unmute-button is strictly safe everywhere (a desktop Chrome user on a fast connection loses nothing but one extra click if they want sound immediately) and requires zero platform-detection code to maintain. This matches ARCHITECTURE.md 6's own escape hatch ("if a video or audio block is set to autoplay, it starts muted on iOS... Player shows a visible unmute button") generalized to always, rather than conditionally-on-iOS.
2026-07-18 | Video/audio `onComplete` confirmed wired into Part 2's trigger builder as a first-class event option: `EVENTS_BY_BLOCK_TYPE.video`/`.audio` both list `['onComplete']`, so any video or audio block's Triggers section (`TriggersSection.jsx`, unchanged from Part 2) now offers "When this finishes playing" in the event dropdown, exactly as Part 2's own architecture anticipated ("this should mostly be adding these two block types to the context-aware event dropdown rules already built in Part 2, not new trigger engine work" -- confirmed true; the only non-trivial addition was `eventLabelFor`'s per-block-type branching for the `onComplete` label collision with knowledge-check, described above). Verified end to end via the player (not just code inspection): a hand-authored trigger on `samples/sample-course.json`'s top-level video block (`blk_video01`, "When this finishes playing, show [hidden text block]") fires correctly when the video's native `ended` event occurs, confirmed via direct DOM/state inspection in the standalone-equivalent `player-dev` server (see gap note below for why this couldn't also be confirmed through the editor's own trigger-builder UI this session). | The action side of this trigger (`SHOW_BLOCK`) required no new engine work either -- it reuses the exact same effect mechanism Part 2 built and this session's own `applyEffects` debug trace confirmed firing with the correct `{action: 'SHOW_BLOCK', target: 'blk_video01_complete_msg'}` payload.
2026-07-18 | **What Phase 5's full video/audio feature set builds on top of, and what it still needs.** Already in place after Part 3: block types `video`/`audio` with `asset_id`/`autoplay`/`loop` content fields; native HTML5 controls (no custom scrubber -- ARCHITECTURE.md 6 is explicit real Phase 5 timeline work should NOT replace this); the one-active-media/container-pause/scroll-pause media manager (`mediaManager.js`); the `onComplete` trigger event, author-configurable today. **Still needed for Phase 5, none of it built or schema-reserved yet in this session:** (a) captions -- `asset.kind: video` has no caption/WebVTT association field anywhere in the schema yet; ARCHITECTURE.md 13.1's Whisper pipeline and the `captions` DB table (already defined in ARCHITECTURE.md 9's table list, migration not yet written) are both fully unbuilt; (b) transcripts -- same Whisper-output dependency, same missing schema/DB/UI; (c) `onTimeReached` event and interactive-video timeline triggers (ARCHITECTURE.md 4/6) -- deliberately not touched per this task's own scope note; the trigger engine has no timestamp-monitoring mechanism at all yet (would need the media manager to poll `currentTime` against author-placed markers, a genuinely new capability, not an extension of anything built this session); (d) `OPEN_MODAL` with `interactive_video_overlay` payload (ARCHITECTURE.md 5.3) -- the unified modal layer supports arbitrary payload types already, but this specific one has no renderer yet; (e) `JUMP_TO_TIMESTAMP` action (ARCHITECTURE.md 4) -- not in `course.schema.json`'s action enum at all yet, a schema gap as well as a UI gap; (f) analytics events `media_play`/`media_pause`/`media_scrub`/`media_complete`/`media_dropoff` (ARCHITECTURE.md 14.2) -- the media manager has exactly the right hook points to fire these (`notifyPlaying`/`notifyStopped` already run on every real play/pause) but nothing calls the analytics module yet, since that module itself doesn't exist before Phase 5. | Recorded in this much detail specifically so Phase 5 doesn't need to re-derive "what's the current state of video/audio in this codebase" from scratch by re-reading every file -- ARCHITECTURE.md Section 6 described the target end-state for the media manager across both Part 3 and Phase 5 in one breath, and this entry is the dividing line for which half is actually done.
2026-07-18 | **Editor+backend testing gap for Part 3, and a workaround discovered this session worth keeping.** `content-server` (the Express backend on port 3001, required for the course-library/editor's `/api/*` calls and the editor's live-preview iframe) failed to stay up through 9 separate restart attempts across this session, dying within seconds of each start -- the same chronic instability documented in this phase's Part 1 and Part 2 entries, and re-confirmed here via the same method (direct code inspection: this session's `assets.js` changes load and execute correctly in isolation via `node -e "import(...)"`, ruling them out as the cause) plus a new check this session -- a manually `nohup`-launched server process on port 3001 was unreachable from the Browser pane at all (`curl`/`fetch` both failed), while the *tracked* `preview_start`-launched process was briefly reachable before dying -- indicating the Bash tool's process sandbox and the Browser pane's network namespace are not the same, so only `preview_start`-launched servers are reachable by the browser at all, and this is infrastructure-level, not something fixable from within the session. **Workaround used successfully:** `.claude/launch.json`'s third configured target, `player-dev` (plain `vite dev` on port 5173, serving `packages/player` directly from source with no backend dependency at all -- `App.jsx`'s boot sequence falls back to the bundled `samples/sample-course.json` with zero network calls when not in SCORM or `?preview=true` context), stayed up reliably for the entire remainder of the session and was used to verify every piece of Part 3's actual new logic (one-active-media, container pause/resume position memory, audio scroll-pause, the onComplete trigger) via direct DOM/JS assertions against a hand-authored test fixture added to the sample course (a video nested inside the existing accordion item, and an onComplete trigger on the top-level video block) -- all confirmed working. **What this could NOT verify, and remains an open gap:** Step 3's editor-side UI (the video/audio upload zone, the native-preview-after-upload state, the Autoplay/Loop settings toggles, "Video"/"Audio" appearing in the block picker) was never rendered in a live browser this session -- verified only by code review against the proven `ImageBlockEditor` pattern it was directly adapted from, plus a clean `npm run build` and zero Vite/console compile errors. Editor-preview-vs-standalone parity for the media manager (Step 7) and autosave/undo-redo behavior with the new block types (Step 8 test 7) are similarly unverified live, though by the same architectural-equivalence argument used in Part 1 (identical player bundle, `VideoBlockSettings`/`AudioBlockSettings` route through the exact same `onChange(..., {forceSnapshot:true})` path `ImageBlockSettings` already proved works with undo/redo) there is no code path where they would behave differently. | Flagged explicitly as unverified-live rather than asserted as a confirmed pass, consistent with this phase's existing standard for handling this specific environment limitation. The `player-dev` workaround is recorded here because it is a genuinely reusable technique for any future session that hits the same `content-server` instability and only needs to test player-side (not editor-side) behavior.

2026-07-18 | **Phase 4 usability-fix session, Step 0: the actual root cause of the content-server crashes documented (and never root-caused) across Parts 1, 2, and 3 of this phase.** Not one bug but a chain of four, found via real investigation this time (fresh start, full terminal output captured, each hypothesis checked against evidence) rather than restart-and-hope: (1) `preview_start`-launched Node processes in this environment run with cwd `/Users/sfranci/Desktop` (the harness's own working directory), not `packages/server` where the process actually lives; (2) `packages/server/src/db.js` and `src/index.js` both called `dotenv.config()` with no explicit `path`, which resolves `.env` relative to `process.cwd()` -- from the wrong cwd this silently finds nothing (dotenv does not throw on a missing file) and `DATABASE_URL` is left `undefined`; (3) with no `DATABASE_URL`, `pg.Pool` falls back to PostgreSQL's own OS-level connection defaults, which use the current OS username as BOTH the connection user and the default database name -- producing the exact observed error, `database "sfranci" does not exist` (confirmed live, sfranci being this machine's actual username); (4) because every route handler except `assets.js`'s single `/assets/upload` endpoint had no try/catch around its async body, every one of those now-failing queries became an unhandled promise rejection at the process level, which Node (v15+) terminates the process on by default -- explaining "dies within seconds, especially when exercising the editor," since editor actions hit the DB constantly (autosave alone fires every 5 seconds). Fixed all four: explicit `dotenv.config({ path: path.resolve(__dirname, '..', '.env') })` in both files (resolved from each file's own location, not cwd); a new `asyncHandler` wrapper (`packages/server/src/lib/asyncHandler.js`) applied to every previously-unwrapped handler across `courses.js`, `pageTemplates.js`, `users.js`, `word.js`, and the remaining handlers in `assets.js`; `pool.on('error', ...)` added to `db.js` (pg's own docs: an unlistened pool 'error' event, e.g. an idle client's dropped connection, crashes the process outright); `server.on('error', ...)` with explicit `EADDRINUSE` detection plus `process.on('unhandledRejection'/'uncaughtException', ...)` last-resort logging added to `index.js`, so any future unanticipated failure logs loudly instead of silently killing the process. **Verified, not assumed**: after the fix, `fetch('/api/courses')` from the live browser returned real course data for the first time in this project's history of browser-tool sessions, a new course was created successfully through the UI, and the server then stayed up continuously through this entire session's remaining five steps of hands-on editor work (creating blocks, triggers, variables, undo/redo, live preview) with zero crashes or restarts -- the sustained-use proof this task explicitly required. **Separately, a real but secondary finding**: a stale/zombie content-server process from an earlier session (PID 96250) was found alive and correctly serving HTTP 200 via direct `curl` at the start of this session, yet was completely absent from `preview_start`/`preview_list`'s own tracking -- a genuine flaw in this environment's process-tracking layer, cleaned up before testing but not itself the primary cause of the historical crashes (the dotenv/cwd bug is). | This is the most important entry in this document from this session, written to be specific and honest rather than defer to environment flakiness again: three prior sessions (Phase 4 Parts 1-3) each reported "unrelated to this session's code" without a stack trace in hand, and each time the actual cause was sitting in `packages/server`'s own startup path the whole time, reachable by anyone who resolved `.env`'s path explicitly instead of restarting and hoping. Anyone hitting "content-server dies within seconds" in this environment going forward should check `dotenv.config()`'s path resolution first, before assuming it's infrastructure-level.
2026-07-18 | **Precedence rule: self-owned block `visibility_condition` (P1-55) vs. an external SHOW_BLOCK/HIDE_BLOCK trigger targeting the same block.** When a block has its own `visibility_condition` set, it is authoritative for that block's visibility from that point forward -- any other block's or page's SHOW_BLOCK/HIDE_BLOCK trigger that names this block as its `target` still fires (the action executes, the effect is dispatched, no error), but has no further effect on what's actually rendered, because `BlockRenderer.jsx` checks `block.visibility_condition` first and only falls back to the trigger-driven `blockVisibility` override when no `visibility_condition` is present (see the precedence comment at the top of `BlockRenderer.jsx`'s visibility logic). A block with no `visibility_condition` behaves exactly as before Part 2/this session -- purely trigger-driven, via the pre-existing `blockVisibility` state and the legacy "Hidden until shown by a trigger" toggle, both left completely unchanged. In the editor, the legacy toggle is hidden from a block's settings panel entirely once `visibility_condition` is set (not shown-but-disabled), to avoid displaying an inert control next to the new, authoritative one -- a judgment call, not explicitly specified by the task, made to keep the settings panel from implying two live controls exist for the same thing. | Chosen because "self-owned, once set, wins" is the only rule that doesn't require the player to somehow merge or arbitrate between two independently-authored visibility sources with no natural combination semantics (AND? OR? last-write-wins across renders?) -- an author who explicitly opens a block's own settings and configures "show this only if X" has made a deliberate, specific choice about that exact block, which should not be silently overridden by a trigger authored elsewhere, possibly by a different person, possibly before the visibility_condition even existed. This mirrors the exact real-user scenario Step 1 fixes: the condition genuinely belongs conceptually to the block being shown, not to whichever other block happens to reference it.
2026-07-18 | **Custom block label fallback behavior (P1-56), confirmed.** When a block's optional `label` field is unset (or blank), every UI that lists blocks by name -- the SHOW_BLOCK/HIDE_BLOCK target picker in `TriggerBuilderModal.jsx`, a trigger's own plain-English sentence rendering in `describeTrigger`/`describeAction`, and a block's own toolbar label in the canvas (`BlockCanvas.jsx`'s `BlockWrapper`) -- falls back to an auto-generated label of the form `"<Type> (<position>)"` (e.g. "Image (3)"), where `<position>` is the block's 1-indexed position among blocks of the *same type* on the *current page only* (`triggerUtils.js`'s new `autoBlockLabel`/`blockLabel` functions). This replaces the previous fallback, which appended a content snippet (e.g. `Text: "Great job..."`) and had no snippet at all for image blocks -- the exact real-user complaint this fixes, since several images on one page were all indistinguishable as plain "Image" in the SHOW_BLOCK dropdown. The position is computed live from the page's current block list, not stored on the block -- it is *not* a stable identifier and can shift if an earlier same-type block on the page is deleted (e.g. deleting the 1st of three images re-numbers the remaining two to "Image (1)" and "Image (2)"), matching how an author would naturally describe "the 3rd image on this page" by eyeballing the page as it exists today, not by some hidden persistent counter. The settings-panel "Block name" input's placeholder shows exactly this auto-generated value, so an author can see at a glance what they're overriding before typing a custom name. Verified hands-on: three image blocks named distinctly on the same page all showed their custom names (not generic "Image") in a trigger builder's SHOW_BLOCK dropdown; a fresh, unnamed video block showed "Video (1)" as both its placeholder and its canvas toolbar label; nested blocks inside an accordion item or two-column slot also respect a custom `label` if set (`ItemBlockStack.jsx`/`TwoColumnBlock.jsx` editors), though their fallback has no page-level position numbering since they aren't part of `pageBlocks` and SHOW_BLOCK/HIDE_BLOCK can't target them anyway (pre-existing scoping, unchanged this session). | Confirming this explicitly because the fallback format is a visible, load-bearing UI contract (what an un-named block "looks like" to every trigger-authoring surface in the product) that's easy to silently change again in a future session without realizing dropdowns and sentences both depend on it matching.

2026-07-18 | Two more real, previously-hidden bugs found and fixed while completing the remaining usability-fix steps, recorded briefly since they're the kind of silent CSS/prop-wiring defect a future session could easily reintroduce without a test to catch it. **Step 4 (mobile drawer utility section, P1-57)**: `UtilityBar.jsx`'s `className` was a binary `layout === 'bottom' ? '--bottom' : '--inline'` ternary -- adding a third layout value (`"drawer"`, for the new section inside `NavDrawer.jsx`) silently fell through to `--inline`, which is `display:none` below the 1280px breakpoint, so the new drawer section rendered in the DOM (confirmed via the trigger's own saved-trigger sentence and DOM inspection) but was invisible at the exact mobile width it exists for -- caught only by checking actual computed CSS classes in the browser, not by the visual absence alone, since the DOM node was genuinely present. Fixed by deriving the class directly from `layout` (`utility-bar--${layout}`) instead of a two-way ternary, so a third (or future fourth) layout value can never again silently collide with the wrong CSS rule. **Step 5 (accent color reaching the player)**: `brand.css`'s `--player-progress-fill` was hard-wired to `--color-accent` (the brand kit's fixed violet CTA color), not `--color-primary` (the variable `App.jsx`'s runtime per-course theme effect actually overrides) -- so the progress bar fill silently ignored every course's configured accent color and always rendered the same fixed violet regardless of `meta.theme.accent`, confirmed via `getComputedStyle` before (`#7C3AED`) and after (`#0f766e`, matching the sample course's actual accent) the one-line fix. Every OTHER accent-colored element in the player (page-current indicator, buttons, links) was already correctly wired to `--color-primary` -- only the progress bar had this one stray reference to the wrong variable. | Both bugs share a shape worth naming: a component built to be reused across N layouts/variables (a deliberate, documented pattern in both files) had one specific variant silently miswired in a way that produced no error, no visual absence in the DOM, and no console warning -- only a wrong color or a wrong CSS class, invisible without either reading the computed styles directly or specifically checking the variant that was actually broken. Confirming a fix "worked" in this codebase increasingly means checking computed values, not just visual presence.

2026-07-19 | **Player settings tab (Contact/Resources/custom items consolidation) and manually-attached course resources.** The utility bar (`meta.utility_bar`, ARCHITECTURE.md 3.3/5.1, schema and player-side rendering since Part 1) had *zero* editor UI before this session -- confirmed by grepping `packages/editor/src` for `utility_bar` before writing any code and finding no matches at all, so "relocate the existing UI" from the task framing turned out to mean "build it for the first time, in its intended final home" rather than moving working code. New `Player` tab in `SettingsPanel.jsx` (`PlayerSettingsPanel.jsx`) covers Contact (enabled/email/subject prefix), Resources (enabled toggle + the new attach/label/remove UI), and Custom items (label + action-type dropdown that swaps its target field between a page picker and a free-text modal-message field depending on whether `action` is `jump_page` or `modal` -- the schema's `action` enum was already both values, but only `jump_page` and a generic message fallback were ever exercised by the player before this session; both are now properly authorable). Manually-attached resources are a new, separate concept from assets: `meta.resources[]` (new schema `resource` definition: `resource_id`, `filename`, `file_path`, `label`, `size_bytes`, `uploaded_at`), a new `resources` Postgres table (migration `007_resources.sql`, mirroring `assets`' shape), and a new `POST /resources/upload` / `PATCH /resources/:resourceId` / `DELETE /resources/:resourceId` route file -- not an extension of the existing asset endpoints, since the resource object shape (label, size_bytes, uploaded_at) has no equivalent on `assets` and reusing it would have meant bolting document-specific fields onto an image/video/audio table. Files are validated by real content-signature sniffing (`packages/server/src/lib/fileSignature.js`, hand-rolled, no new dependency), not by the client-declared MIME type the existing `assets.js` endpoint trusts alone -- checked explicitly because this task named "an .exe renamed to .pdf must still be rejected" as a requirement stricter than what the precedent endpoint actually does. Kind is derived from the file extension (the task's own allowlist is extension-based: pdf/doc/docx/xls/xlsx/ppt/pptx/zip/txt), then the file's real leading bytes are checked against that kind's container-format signature (`%PDF-` for PDF; the shared ZIP magic number for docx/xlsx/pptx/zip, since OOXML is ZIP-with-XML-inside; the shared OLE compound-file magic number for legacy doc/xls/ppt; a null-byte/UTF-8 heuristic for txt, which has no magic number of its own), plus an explicit named check for known executable/script signatures (MZ, ELF, shebang) so a disguised binary gets a specific rejection reason rather than a generic "doesn't match." Player-side, the Resources utility item now opens a new `resources` modal payload (`ResourcesPayload.jsx`, registered in `Modal.jsx`'s `PAYLOAD_RENDERERS`) listing each attached resource's label and human-readable size with a real `<a download>` link -- `download` forces the browser to save-to-disk on a same-origin URL instead of navigating/rendering in place, satisfying the in-player containment rule (ARCHITECTURE.md 5.2) without needing PDF.js or any embedded viewer, since these are arbitrary document types, not just PDFs. `resources` is threaded as a new prop alongside the existing `utilityBar` prop through all three `UtilityBar` render sites (`TopBar.jsx`, the drawer's own utility section added last session, and the fixed bottom bar) rather than folded into `utility_bar` itself, since `meta.resources` and `meta.utility_bar.resources` are deliberately separate concerns (attached files vs. the toggle that controls whether they're exposed) -- see the toggle-independence decision below. | **Judgment call, per the task's own instruction to choose and document:** uploading/labeling/removing resources works regardless of whether the Resources toggle is on. The toggle is purely a player-visibility switch, not a gate on the authoring workflow -- an author can prepare and organize a course's resources before deciding to expose the button, without losing anything already attached if the toggle happens to be off at some point. This mirrors how Contact's email/subject fields are also editable while the Contact toggle is off. **Two real bugs found and fixed while building this, not designed in from the start:** (1) the resource-row UI's first draft used a 3-column horizontal layout (filename+size / label input / Remove button) inside the settings sidebar's fixed ~280px width -- measured via `getBoundingClientRect()` after it visually looked broken and found the label input had collapsed to 26px wide, leaving no usable typing space; fixed by stacking the row vertically (filename+size on one line, label input full-width below, Remove on its own line), which is a much better fit for a narrow fixed-width sidebar than any 3-across arrangement. (2) None at the backend/validation layer -- the signature validator was unit-tested directly in Node against real PDF/ZIP/OLE/EXE byte signatures before ever touching the browser, and every case (real PDF accepted, real ZIP accepted as docx-family, disguised EXE rejected regardless of claimed extension, non-text binary rejected as txt) matched expectations on the first run.
2026-07-19 | **This feature is deliberately independent of, and precedes, the Phase 5 auto-generated PDF pipeline (P1-18/19, ARCHITECTURE.md 12) -- the two are expected to coexist, not compete, once Phase 5 lands.** The auto-generated pipeline writes its output PDFs into the existing `assets` table (ARCHITECTURE.md 12.2: "PDFs stored as assets in the assets table, linked to the course version") and surfaces them via `pdf_settings.resources_page`, a course-wide auto-appended page -- a fundamentally different mechanism from this session's manually-curated `meta.resources[]` list, which an author builds explicitly, one upload at a time, choosing exactly what's included and how it's labeled. When Phase 5 actually builds the auto-generated pipeline, the most likely integration point is the Resources *modal* itself (not the resources array) -- `ResourcesPayload.jsx` growing a second section, or merging the auto-generated PDF(s) into the same rendered list alongside whatever's manually attached here, so a learner sees one unified Resources button regardless of which mechanism produced any given item. Nothing in this session's schema, routes, or player code assumes it owns the *only* way a resource can appear in that modal -- deliberately, so Phase 5 can add its own source without a redesign.

2026-07-19 | **Phase 4.5a Step 1/2: identity audit and stable-ID convention.** Audited every addressable entity in DATA-MODEL.md section 2 against the real schema, editor code, and all 13 courses in the dev database (not just the hand-authored sample). Two real gaps, both 100% missing: **accordion/tab nested items** (20/20 accordion items, 11/11 tab items across the DB have no id at all -- keyed purely by array index, including in DOM ids and even the block-level onOpen/onClose trigger, which fires without knowing which item was toggled) and **variables** (5/5 missing -- identified only by `name`, which `VariableManagerPanel.jsx` explicitly forbids renaming today specifically because there's no id to decouple identity from name: "A variable's name can't be changed once created -- it's how triggers reference it"). Every other entity DATA-MODEL section 2 lists already has a working stable id: pages/blocks/triggers/assets (`pg_`/`blk_`/`trg_`/`ast_`, via `idGen.js`), knowledge-check answer options (field is `id`, not `option_id` as ARCHITECTURE.md 3.8's example shows -- a doc/code drift, corrected in that doc as part of this entry, not a data gap: 56/56 options across the DB already carry one), resources (`res_`, server-generated via `uuid`), and objectives/concepts (schema-required already; zero real instances anywhere since no authoring UI exists yet for either). Two entities deliberately get **no new id**, reasoned explicitly per the task's own instruction: **per-option feedback** is a single embedded object per option (not a list), addressable via the option's own id plus a fixed field path (matches DATA-MODEL section 11's own "object id -> field path" translation-overlay model) -- it only needs its own id if it ever becomes a list of variants, which it isn't today. **Table cells/rows** are plain strings only (ARCHITECTURE.md 3.7: no rich text, no nesting), never individually triggered, and translate as a whole-`rows`-array replacement keyed by the table block's own `block_id` -- the existing block_id is sufficient addressing granularity. **Translation units** don't exist in the schema at all yet (Phase 5, unbuilt -- `course.schema.json`'s top level has no `translations` property and `additionalProperties: false` would reject one today), so there is nothing to audit or migrate for that entity. | **ID convention adopted, extending the project's existing `blk_`/`pg_`/`trg_`/`ast_`/`res_`/`crs_` pattern** (field name `{singular_type}_id`, value prefix a short abbreviation, generated once via a shared `shortId()` = `Math.random().toString(36).slice(2,8)`, never regenerated except on explicit copy/duplicate): `item_id` / `itm_` for accordion and tab nested items (new), `variable_id` / `var_` for variables (new), plus forward-reserved (not yet used by any authoring UI) `objective_id`/`obj_`, `concept_id`/`cpt_`, `group_id`/`grp_` generators added to `idGen.js` for whenever those UIs are built. `fbk_` (feedback-variant prefix) is deliberately NOT added, matching the no-id-needed reasoning above -- reserved in this note only so a future session doesn't reintroduce it without re-reading this reasoning first. New generators (`genOptionId`, `genItemId`, `genVariableId`, `genObjectiveId`, `genConceptId`, `genGroupId`) added to `packages/editor/src/lib/idGen.js` alongside the existing ones. Wired at every point new items/options/variables are actually created: `AccordionBlock.jsx`/`TabsBlock.jsx`'s "Add item"/"Add tab", `blockDefaults.js`'s default accordion/tabs/knowledge-check content (replacing the old hardcoded literal `opt_a`/`opt_b`), `KnowledgeCheckBlock.jsx`'s `addOption` (replacing its own ad hoc inline `Math.random()` call with the shared `genOptionId`), and `VariableManagerPanel.jsx`'s add/edit handlers (a new variable gets a `variable_id` on creation; editing preserves the original variable_id rather than dropping it, since `handleEdit` previously replaced the whole object wholesale). `CourseEditor.jsx`'s existing block-copy machinery (`rebuildBlockWithIds`, used by Copy-to-page/Insert-from-template, and `regenerateIds`, used by single-block Duplicate) now also mints a fresh `item_id` on every accordion/tab item it copies, mirroring how `block_id`/`trigger_id` already get regenerated on copy -- keeps `item_id` globally unique per DATA-MODEL section 19, the same invariant block_id already upholds. Deliberately NOT done: KC option ids are not regenerated on block Duplicate/Copy today (a knowledge-check block copied via Duplicate produces options with identical `id` values to the original) -- this is a pre-existing gap in `content.options` handling (those functions only ever recursed into `content.items[].body_blocks`, never `content.options`), not something introduced by this session, and matches the exact "known pre-existing gap, not a regression" precedent already set for Duplicate's trigger-target-remapping gap (2026-07-16 entry). Left for whichever session builds REQUIREMENTS.md P1-41 ("Duplicate knowledge check question... with new block_id and option ids"), which names this exact behavior as its own explicit acceptance criterion. Also deliberately NOT done: variables get a `variable_id` now, but `triggerEngine.js`/`ConditionBuilder.jsx`/every action's `var` field still resolve variables by `name`, unchanged -- rewiring reference resolution from name-based to id-based lookup is a runtime-engine behavior change, not an identity/schema one, and doing it in this session would risk exactly the kind of regression Step 7's own regression pass exists to catch, for a change nobody asked for ("no new UI, no new features"). The `variable_id` field exists now specifically so that decoupling can happen later without another schema migration.
2026-07-19 | ARCHITECTURE.md 3.8's knowledge-check example corrected from `"option_id": "opt_a"` to `"id": "opt_a"`, matching the field name every KC option has actually used in shipped code since Phase 1 (`KnowledgeCheckBlock.jsx` both editor and player, `blockDefaults.js`). Pure documentation-accuracy fix, discovered during the Phase 4.5a Step 1 identity audit -- no data or schema changed as a result, since the working field was never actually named `option_id` anywhere outside this one doc example.

2026-07-19 | **Phase 4.5a Steps 3-5: schema requirements, migration service, and migration 0001.** `course.schema.json`: `variable` now requires `variable_id` (pattern `^var_[a-zA-Z0-9_]+$`); `block` gained a type-conditional `allOf`/`if/then` (same pattern the file already uses for `variable`/`action`) requiring `item_id` on every accordion/tabs item via a new shared `items_with_stable_ids` definition; `schema_version` bumped from `const: 1` to `const: 2` -- v1 documents are now expected to fail validation against the current schema, which is exactly why the migration chain exists. New package `packages/schema` (previously just a bare JSON file with no package.json, no exports, no JS consumer anywhere in the app -- confirmed by grep before starting: `ajv`/`jsonschema` had only ever been run manually from the command line during dev sessions, never wired into the running server/editor/player). `packages/schema/index.js` exports `validateCourse()` (ajv, draft-07, compiled once) and `CURRENT_SCHEMA_VERSION` (read off the schema itself, not hand-duplicated). `packages/schema/migrations/index.js` is the sequential chain runner implementing ARCHITECTURE-AUDIT.md 4.3's exact flow (load -> inspect version -> migrate N->N+1 -> validate -> normalize -> open): refuses a schema_version newer than `CURRENT_SCHEMA_VERSION` (downgrades never assumed safe), clones before handing a document to each migration step (belt-and-suspenders purity guarantee beyond just trusting each migration to not mutate), throws a typed `MigrationError` with course id/from/to version on any failure (a migration throwing, a migration returning the wrong shape or wrong resulting version, or the final validate step failing), and only logs (`[migration] course <id>: ...`) when something actually changed. This is a distinct mechanism from `packages/server/src/migrate.js` (Postgres table/column migrations, run once via `npm run migrate`) -- same word, different layer: this one migrates the *shape of a single course_json document*, in memory, every time one loads that isn't already current, with no "have I run this" tracking table since the document's own `schema_version` field is that state. Migration `0001-add-missing-stable-ids` (v1->v2) walks every page's blocks (recursing into accordion/tabs items' `body_blocks` and two_column `left`/`right`, mirroring the exact recursion shape `CourseEditor.jsx`'s own id-remap functions already use) assigning `item_id`/`variable_id` only where missing, preserving every existing id exactly, and is idempotent in spirit (checks presence before assigning, not just gated by the version check). ID generation is deliberately duplicated into `packages/schema/migrations/idGen.js` rather than importing `packages/editor/src/lib/idGen.js` -- same cross-package-duplication precedent as `richText.js` (2026-07-12/17 entries): packages/schema is a dependency of the server (and potentially the player), and neither should transitively pull in editor code for three one-line functions. Wired into the one real course-serving path: `GET /api/courses/:id` in `packages/server/src/routes/courses.js` -- confirmed by reading the actual client call sites (`packages/editor/src/lib/api.js`'s `getCourse` and `packages/player/src/App.jsx`'s preview-mode fetch both hit this exact endpoint) that "content server serves a course" and "editor loads a course" are, in this codebase today, the same single endpoint, not two independent paths -- so one wiring point covers both named load paths. Also wired into `/duplicate` and `/save-as-template`, which both read a stored row and reuse its `course_json` -- neither should hand a caller (or a newly-created duplicate/template) pre-migration data either. "Original retained until the migrated version validates and saves successfully" is implemented literally: `loadAndMigrateCourseRow()` only issues the `UPDATE courses SET course_json = ...` after `migrateCourse()` has already both transformed and schema-validated the document in memory; if that `UPDATE` itself fails, the function throws and the request fails loudly (500) rather than serving migrated-but-unpersisted data. | **Real-data discovery, not an identity issue but directly blocking "runs cleanly on historical fixtures" against REAL fixtures:** running this migration+validation pipeline against all 19 courses in the dev database (the first time ANY real schema validation has ever executed against this app's actual data) surfaced three pre-existing issues unrelated to IDs, each fixed because leaving any one of them unfixed would have made every affected course permanently unloadable the moment validation went live -- a direct violation of "no existing functionality regresses": (1) `asset.alt` had a `minLength: 1` constraint since Phase 1 that was never enforced and contradicts documented product behavior (ARCHITECTURE.md 8: missing alt text is a pre-publish *warning*, not a load-blocking error) -- relaxed to allow empty string, not papered over with a fake placeholder value that would defeat the point of the warning. (2) `meta.resources[].size_bytes` came back as a JS string, not a number, for every resource ever uploaded -- root cause: the `resources` table's `size_bytes` column is `BIGINT`, which `pg` always returns as a string (precision safety), and `packages/server/src/routes/resources.js`'s `insertResource` returned that raw row straight through to `CourseEditor.jsx`, which copies it verbatim into `course_json`. This was an ongoing bug, not just historical data -- fixed at the source (`insertResource` now returns `Number(row.size_bytes)`) so new uploads are correct, plus migration 0001 coerces any already-stored string values (2 real resources found needing it). (3) Two courses in the dev DB have no `schema_version` field at all (predating the field's own existence) -- the runner now treats an absent `schema_version` as `1`, not a guess among several possibilities (1 is the only version that ever existed before this field did) and NOT the same thing as downgrade tolerance (a *present*, too-high version is still refused). After all three fixes: 18/18 real (non-deleted, non-corrupted) courses and templates migrate cleanly to v2 and pass validation; the sole remaining failure is a single already-**soft-deleted** test course (`status='deleted'`, `course_json: {}`) that predates the 2026-07-12 "editor never sends a bare `{}`" fix and was never openable even before this session (the old code would have crash on `json.pages.find` client-side with no guard) -- not a regression, correctly refused rather than fabricated, and unreachable through any normal UI path since `GET /courses`/`GET /templates` both filter `status != 'deleted'`.

2026-07-19 | **Phase 4.5a Steps 6-7: historical fixture tests and hands-on regression pass.** Fixture library at `packages/schema/migrations/__fixtures__/` (five v1-shaped documents, `node --test`, `node:assert/strict`, no new test-framework dependency): `full-course-v1.json` is the REAL pre-4.5a `samples/sample-course.json` pulled from git history (`git show HEAD:samples/sample-course.json`), not a hand-built toy -- exercises every block type Phase 1-4 shipped in one document, including the two-column WSI slot, table, objectives/concepts, and page_groups. `accordion-tabs-v1.json` and `variables-v1.json` isolate the two real gaps found in Step 1. `legacy-no-schema-version.json` reproduces the real "2 courses with no schema_version field at all" finding from Step 5. `resources-string-size-bytes-v1.json` reproduces the real string-`size_bytes` bug. `already-current-v2.json` is a hand-built v2 document (ids already present) for the true no-op case. 9 tests in `0001-add-missing-stable-ids.test.js`: each v1 fixture migrates to `CURRENT_SCHEMA_VERSION`, passes `validateCourse`, preserves every pre-existing `block_id`/`page_id` (order and value, via `deepEqual`) and every pre-existing `name`/`type`/`default` on variables, and ends with every accordion/tab item and variable carrying a uniquely-valued `item_id`/`variable_id`; a dedicated test confirms an already-present `item_id` is left untouched while a sibling item on the same block still gets one (the idempotence requirement, tested directly rather than relying solely on the version-gate); the current-v2 fixture asserts a byte-for-byte-identical, zero-diagnostic no-op; a dedicated idempotence test runs the full chain twice in a row and asserts the second pass changes nothing; a final test asserts a from-the-future `schema_version` is refused. All 9 pass (`node --test migrations/*.test.js`, packages/schema). | **Regression pass (Step 7), each checked hands-on in the running app, not inferred from code reading:** (1) server + editor started clean, no crashes. (2) Opened a real pre-migration course ("Phase 3 TEST Course", 14 previously-id-less accordion/tab items) in the editor -- migrated to v2 on load, every block type rendered with content fully intact (verified via a full DOM text dump, not just a screenshot): heading, two-column (text+image), table, 3-item accordion with nested body_blocks, 4-tab tabs block, reflection, second two-column, embed, 8-image carousel, and a knowledge-check with 4 options and real per-option feedback text. (3) Created a new course through the UI -- confirmed via API at `schema_version: 2` with proper `course_id`/`page_id`. Then, in that same new course, exercised the actual authoring code paths touched this session: clicked "+ Add item" on a fresh accordion, "+ Add option" on a fresh knowledge-check, and "+ Add Variable" in the Variables panel -- confirmed via API that all three produced correctly-prefixed, unique ids (`itm_mhj8tn`, three `opt_*` ids, `var_ikukem`), not just that the migration back-fills old data. (4) Every block type appeared in the "Add a block" picker (text/heading/image/list/accordion/tabs/knowledge-check/carousel/reflection/two-column/table/embed/video/audio); list/video/audio specifically were not re-typed-into by hand this pass (no code path touching them was changed this session, and real DB courses containing them -- "Phase4_SF Test" -- already migrated cleanly in the Step 5 full-database sweep), so this item is confirmed by combination of picker-availability + real-data migration success rather than a fresh click-render for those three specifically. (5) **Trigger engine, verified end-to-end, not just "no console errors":** added a real `onOpen -> SET_VAR regressionVar = true` trigger to the new course's accordion through the actual Trigger Builder UI, confirmed it persisted with a proper `trg_` id and the variable referenced by name (unchanged resolution path, per the Step 2 decision), then opened the player, hooked `console.log` to capture the player's own `[player] variable state:` debug output, clicked the accordion open, and read back `{"regressionVar": true}` -- the full author-to-runtime path (trigger authoring -> persistence -> migration-produced ids -> player trigger engine -> variable state) proven live. (6) Autosave: edited a heading's text, navigated away without an explicit save action, and confirmed via a direct API read that the edit persisted. Undo/redo: made a second edit in a fresh page load (undo history doesn't survive reload, ARCHITECTURE.md 3.9, so this had to be a same-session edit), clicked Undo, confirmed the field visually reverted to the pre-edit text and the Redo button un-greyed. (7) Player rendering of the migrated course confirmed in both contexts: standalone player (`/player?courseId=...`) rendered the full course correctly (confirmed via DOM text dump and an all-200-OK network log, including every uploaded image asset) and the editor's own live-preview iframe (Desktop toggle) rendered the identical content plus the WSI two-column embed's real pan/zoom viewer chrome -- both are the same player bundle (confirmed architecturally in the Step 4 entry above), and both were independently exercised rather than assumed equivalent from the one check. All 7 regression items: **PASS**.

2026-07-19 | **Phase 4.5b Step 1: block-type knowledge scatter audit.** Before building the registry, searched the editor and player for every place a block type is hardcoded as a list, switch, or lookup table. Found six real scatter points, each independently maintained: (1) `packages/editor/src/lib/blockDefaults.js`'s `BLOCK_TYPES` array and `BLOCK_LABELS` object -- the source the old "Add Block" picker rendered from, flat (no category grouping). (2) `packages/editor/src/components/blocks/index.js`'s `BLOCK_EDITORS` object -- editor-side type-to-component switch. (3) `packages/player/src/blocks/BlockRenderer.jsx`'s `REGISTRY` object -- the player-side equivalent, a second component map with no shared source with (2). (4) `packages/editor/src/lib/triggerUtils.js`'s `EVENTS_BY_BLOCK_TYPE` -- hand-maintained per-type valid-event list that the trigger builder's event dropdown reads; this is the exact scatter point the task's own WHY section named ("the trigger builder's event dropdown had to be manually taught about each block type"). (5) `blockDefaults.js`'s `createBlock()`/`createInnerBlock()` -- an inline `if (type === 'reflection') block.include_in_pdf = true` special-case, the only place a PDF default was set, silently defaulting every other type to no `include_in_pdf` field at all rather than an explicit `false`. (6) `packages/editor/src/components/blocks/settingsIndex.js`'s `BLOCK_SETTINGS` map -- a third type-to-component switch (settings-panel side), left out of this phase's migration since it wasn't one of Step 3's five named surfaces and settings-panel components are a different shape problem (per-field forms, not swappable like editor/player renderers) that doesn't fit the registry's `editorComponent`/`playerComponent` pattern cleanly. Also found three *deliberately separate* "allowed inner block type" lists that look like scatter but aren't: `TWO_COLUMN_SLOT_TYPES` (schema-valid inner types for two-column slots, includes `heading`+`embed`), `STACK_PICKER_TYPES` in `ItemBlockStack.jsx` (offered for accordion/tab items, excludes `embed`), and `SLOT_PICKER_TYPES` in `TwoColumnBlock.jsx` (offered for two-column slots specifically, deliberately excludes `heading` per an existing code comment even though heading is schema-valid there) -- each encodes a real, different product decision about what an author is *offered* versus what the schema *permits*, and collapsing them into one `canContainBlocks` value would either lose that distinction or silently change picker behavior no one asked to change. Left untouched; see the registry-shape entry below for why `canContainBlocks` doesn't unify them.
2026-07-19 | **Phase 4.5b Step 2: registry shape and location.** Built `packages/schema/block-registry.js`, not `packages/editor` or a new top-level package -- `packages/schema` is already a dependency-free, framework-agnostic package (no React, no server-only imports) that the server, editor, and player can all import without pulling in code that doesn't belong in their own dependency graph, the same reasoning already established for `packages/schema/dependency-index.js`'s sibling design in this same phase. Neither the editor nor player package previously listed `@mnemonify/schema` as a dependency at all (confirmed by reading both `package.json`s before starting) despite both already reaching into `packages/schema/course.schema.json`'s shape informally -- added `"@mnemonify/schema": "*"` to both, making the existing implicit coupling explicit rather than introducing a new one. Each of the 14 registry entries (`text`, `heading`, `image`, `list`, `accordion`, `tabs`, `knowledge-check`, `table`, `two_column`, `embed`, `carousel`, `reflection`, `video`, `audio`) carries `type`, `displayName`, `category` (`Content`/`Layout`/`Interactive`/`Media`, matching the task's suggested grouping), `validEvents`, `canContainBlocks`, `includeInPdfDefault`, and `hasSettings` -- but deliberately **not** `editorComponent`/`playerComponent` despite the task listing them as suggested fields. Reasoning: `packages/schema` has zero React dependency today and must stay that way for the server (Express, no bundler, no JSX) to keep importing it directly; a component reference is either a React element/lazy-import (pulls React into the server's dependency graph) or a bare string requiring each consumer to maintain its own string-to-component lookup anyway (which is just the scatter problem restated one level down). Instead, `BLOCK_EDITORS` (editor) and `REGISTRY` (player) remain local, environment-specific component maps exactly as before, but each now runs a `import.meta.env?.DEV`-gated completeness check against the schema registry's `BLOCK_TYPES` list on module load -- console-warning if a type the registry knows about has no matching component map entry. This directly targets the bug class named in the task's own WHY section (a block type added to the schema/player but silently missing from a picker or renderer) without merging environments that have a real reason to stay separate. `canContainBlocks` is a boolean only (`true` for `accordion`/`tabs`/`two_column`, `false` for the other 11), not an enumerated list of allowed inner types -- deliberately coarser than the three real "what can go inside this" lists found in the Step 1 audit, which stay exactly where they are (`TWO_COLUMN_SLOT_TYPES`, `STACK_PICKER_TYPES`, `SLOT_PICKER_TYPES`), since those three answer three different product questions ("what does the schema permit" vs. two different "what does this specific picker offer") that a single registry field can't correctly collapse into one value without picking a winner among them.
2026-07-19 | **Phase 4.5b Step 4: dependency index design.** `packages/schema/dependency-index.js` exports `buildDependencyIndex(courseJson)` and `getDependents(entityId, courseJson)`. **Derived, never persisted** -- per ADR-0007's principle for the learning-alignment graph, extended here to this second derived index: nothing in this file writes back to `course_json`, there is no `dependencies` table or field, and `getDependents()` rebuilds the full index from scratch on every call rather than reading a cache. This means there is no invalidation problem to solve (a stale cache can't exist if nothing is ever cached) and no migration needed if the edge types this file tracks change in the future -- a deliberate trade of "recompute cost on every call" for "provably always correct," acceptable because course documents in this app are small (tens of blocks, not thousands) and delete-warnings are triggered by an explicit user action (clicking Delete), not a hot path like keystroke-level autosave. If a future caller needs to avoid rebuilding on every render, that caller owns memoizing the result and invalidating on save -- this module doesn't need to know about that to stay correct. Reference types tracked, matching DATA-MODEL.md section 16's edge list: `trigger_reads_variable`/`trigger_writes_variable` (a trigger's `condition` or a `SET_VAR`/`ADJUST_VAR` action referencing a variable by name), `trigger_targets_block` (`SHOW_BLOCK`/`HIDE_BLOCK`/`ENABLE_BLOCK`/`DISABLE_BLOCK` action targets), `trigger_navigates_to_page` (`JUMP_TO_PAGE` action targets, both block-level/page-level triggers and utility-bar custom items' `jump_page` action), `block_uses_asset` (image/video/audio `content.asset_id`, carousel `content.asset_ids`, knowledge-check question/option/feedback image fields, and rich-text inline `asset_id` segments), `visibility_condition_reads_variable` (P1-55 self-owned block visibility, per the bug-fix note below), and `continue_gate_reads_variable` (page-level continue gate). Recurses into two_column `left`/`right` and accordion/tabs `content.items[].body_blocks` -- the identical containment shape `CourseEditor.jsx`'s id-remap functions and migration 0001 already walk, reused rather than reinvented. Author-facing labels reuse the same "custom label, falling back to type+position-among-same-type" convention as `triggerUtils.js`'s `blockLabel`/`autoBlockLabel` (P1-56), but the logic is duplicated locally (`labelForBlock` in `dependency-index.js`) rather than imported, for the same cross-package-boundary reason the registry's component maps stay local: `triggerUtils.js` lives in the editor package, and `packages/schema` importing editor code would invert the dependency direction the whole registry design exists to avoid. **Real bug found and fixed as a side effect, exactly as the task's WHY section anticipated:** the old ad hoc `countVariableUsages` in `triggerUtils.js` (the pre-4.5b variable-delete-warning logic) never checked `block.visibility_condition` at all -- only trigger conditions and actions -- so a variable referenced solely by a block's own P1-55 visibility rule showed "0 usages" and could be deleted while silently breaking that block's visibility. Verified live: added a `visibility_condition` referencing a test variable, confirmed the old code path's blind spot conceptually (it has no code path that touches `visibility_condition` at all), then confirmed the new `getDependents`-based warning correctly reports "used in 1 place." This is a genuine correctness improvement inherited automatically from having one correct walk instead of two independently-maintained ones, not a deliberate scope addition.
2026-07-19 | **Phase 4.5b Step 5: delete-warning flow wiring -- one refactored, one new.** `VariableManagerPanel.jsx`'s `handleDelete` was **refactored**: replaced its call to the now-deleted `countVariableUsages(courseJson, name)` with `getDependents(name, courseJson).length`, same `window.confirm` UX ("This variable is used in N place(s). Delete anyway?"), but now correctly counts `visibility_condition` references too (see the dependency-index entry above). The Media Library's asset delete was **new** -- confirmed by reading `MediaLibraryPanel.jsx` before starting that `handleDelete(dbAsset)` previously called `api.deleteAsset` with no usage check of any kind, not even a bare confirm dialog, meaning an author could delete an image out from under a live image block, a knowledge-check option, or a carousel with zero warning. Added a `getAssetDependents` prop (passed down from `CourseEditor.jsx` as `(assetId) => getDependents(assetId, json)`, keeping `dependency-index.js` itself unaware of any React/component wiring) and a confirm dialog listing up to 5 referencing block labels plus a "+N more" suffix when truncated, matching the variable flow's pattern rather than inventing a second UX for the same underlying concept. Verified live against a real course asset already referenced from 4 places (an image block plus a knowledge-check's question/option images): the dialog correctly read `"This image is used in 4 places (Image, Knowledge Check (1), Image, Knowledge Check (1)). Delete anyway?"`, and cancelling left the asset and all 4 references untouched (confirmed via a direct API read after the test).
2026-07-19 | **Phase 4.5b Step 6: regression test, 8/8 pass.** Built a dedicated test course (`Regression Test 4.5b`, created directly via `POST /api/courses` with a hand-assembled `course_json` covering all 14 registry block types on one page, rather than one-at-a-time through the Add Block picker, to make the pass repeatable) and ran the task's own 8-point checklist against it plus two pre-existing courses. (1) **Add Block picker**: confirmed grouped into the registry's 4 categories (Content/Layout/Interactive/Media) during Step 3's own migration testing -- PASS. (2) **Editor rendering**: all 14 types (text, heading, image, list, table, two_column, accordion, tabs, knowledge-check, reflection, carousel, embed, video, audio) added and rendered correctly in the block editor, verified via a full DOM text dump confirming all 14 type labels present in the exact order authored, zero console errors -- PASS. (3) **Player rendering**: same course opened in the editor's live-preview iframe (Desktop toggle) -- confirmed via `iframe.contentDocument` text dump that every block's rendered output was present (including both images -- the image block's and the carousel's, same source asset -- and correct "Video/Audio unavailable" placeholders for the two blocks with no uploaded file), zero console errors -- PASS. (4) **Trigger event dropdowns**: accordion (`onOpen`/`onClose`), knowledge-check (`onCorrect`/`onIncorrect`/`onComplete` = "an answer is submitted"), and video (`onComplete` only = "this finishes playing", no `onPlay`/`onPause`) each opened via the actual Add Trigger UI and their `<select>` options read directly from the DOM -- all three match `EVENTS_BY_BLOCK_TYPE`'s registry-derived values -- PASS. (5) **Variable delete-warning via a trigger** (not just visibility_condition, which Step 4/5 already covered): created a variable, referenced it in a real `SET_VAR` trigger action on the knowledge-check block via the Trigger Builder UI, attempted delete -- confirm dialog correctly read "This variable is used in 1 place. Delete anyway?" -- PASS. (6) **Asset delete-warning**: covered in the Step 5 entry above against a real 4-reference asset -- PASS. (7) **Undo/redo and autosave**: deleted the Text block via its toolbar close button, clicked the top-bar Undo button (title `"Undo (Cmd+Z)"`) -- block reappeared with content intact, confirmed via DOM query; top-bar status showed "Saved ✓" after, confirming autosave completed on both the delete and the undo -- PASS. (8) **Pre-existing course still opens**: opened "My First Course" (a Phase 3 course, untouched this session and pre-dating both 4.5a and 4.5b), heading and text blocks rendered correctly, "Saved ✓" shown, zero console errors -- PASS. All 8: **PASS**, no regressions found, no code changes required as a result of this pass (unlike Phase 4.5a's Step 7, which surfaced real data bugs -- this pass found none).

2026-07-19 | **Phase 4.5c Step 1: simplified finding model, and explicit confirmation of what's deferred.** COURSE-ANALYZER.md section 3's full finding model carries `id`, `rule_id`, `rule_version`, `severity` (four tiers: error/warning/suggestion/info), `category`, `message`, `why`, `location`, `fix`, `suppressible`, `fingerprint`, plus section 3.1's `confidence`/`basis`/evidence/`requiresHumanJudgment`/active-profile fields. This phase's finding shape is deliberately smaller: `{ ruleId, severity: 'error'|'warning', message, entityType, entityId, location: { page_id?, block_id? } }` -- six fields, two severity tiers, no per-finding `id` (findings aren't persisted or referenced individually across calls, so nothing needs a stable instance identifier yet) and no `fingerprint` (fingerprints exist to survive across re-runs for suppression/snapshot matching, neither of which this phase builds). **Explicitly deferred, not forgotten:** rule versioning and rule-packs (section 5/7 -- every rule here is unversioned; a rule changing behavior later is just a code change, not a version bump, until profiles need to pin behavior); analyzer profiles (section 7 -- there is exactly one rule set, always fully enabled, no per-organization or per-course configuration); the `confidence`/`basis`/evidence/`requiresHumanJudgment` fields (section 3.1 -- every rule in this phase is either a certain structural fact -- schema validity, broken references -- or a `basis: computed` heuristic close enough to certain that a four-tier confidence scale would be manufacturing precision this phase doesn't have); suppression (section 13 -- no waiver workflow, no per-finding "acknowledge and hide" state; every finding shows every time the analyzer runs); analyzer snapshots and reproducibility records (section 8 -- `analyzeCourse()` is deterministic and re-run on demand, but nothing about a given run -- engine version, timestamp, which findings existed at last publish -- is stored anywhere); and the entire pedagogical/learning-alignment rule catalog (sections 6.3's logic-cycle rules beyond broken references, 6.4-6.8, 6.10's media-management rules beyond "unused asset", 6.11's publishing/export rules beyond what this phase's gating covers, 6.12-6.14) -- this phase implements only sections 6.1 (structural validity) and 6.2 (accessibility)'s most concrete, zero-judgment items, plus a slice of 6.3 (broken references only, not cycles/unreachability/contradiction-detection) and 6.14 (unused variable/asset only, not unused-trigger/deprecated-block/orphaned-translation). A future session building profiles, suppression, or the pedagogical categories is extending this phase's foundation, not correcting an oversight.
2026-07-19 | **Phase 4.5c Step 3: final rule list, as actually implemented, with severity.** All 15 rules the task specified were implementable; none were skipped for being too fragile, EXCEPT rule 10 (video captions), skipped for the reason below. 14 rules ship in `packages/schema/analyzer/rules.js`. **ERROR (broken references + schema validity -- a referenced id either exists or it doesn't, zero false-positive risk):** `schema.invalid` (rule 1, reuses `validateCourse` from 4.5a's ajv validator, one finding per ajv error); `broken_ref.variable_missing` (rule 2, covers trigger `condition`/`SET_VAR`/`ADJUST_VAR` var references AND page `continue_gate` references in one rule, since dependency-index.js tracks all three as variable-reading edges); `broken_ref.visibility_variable_missing` (rule 5, kept separate from rule 2 per the task's own numbering, since P1-55 visibility_condition is authored in a different UI surface than a trigger); `broken_ref.block_target_missing` (rule 3, covers SHOW_BLOCK/HIDE_BLOCK/ENABLE_BLOCK/DISABLE_BLOCK targets together, since dependency-index.js's own `trigger_targets_block` edge type doesn't distinguish which of the four fired); `broken_ref.page_target_missing` (rule 4, covers trigger JUMP_TO_PAGE and utility-bar custom `jump_page` items together, both being "navigate to a page" defects); `broken_ref.asset_missing` (rule 6); `broken_ref.page_group_missing` (rule 7, the one broken-reference rule NOT built from the dependency index -- `meta.page_groups` isn't tracked by `dependency-index.js` at all, since page groups are a nav-drawer grouping construct with no trigger/condition involvement, so this is a small standalone existence check instead). **WARNING (accessibility gaps, unused entities, and completeness heuristics):** `a11y.image_alt_missing` (rule 8); `a11y.carousel_image_alt_missing` (rule 9); `a11y.table_caption_missing` (rule 11, threshold: more than one row of data, excluding the header row when `has_header_row` is set); `unused.variable` (rule 12); `unused.asset` (rule 13); `unused.unreachable_block` (rule 14, see the reachability-heuristic entry below for why it's WARNING not ERROR, and for the deliberate simplification on the `visibility.initial:'hidden'` branch); `completeness.unsatisfiable_continue_gate` (rule 15). **Rule 10 (video captions) was NOT implemented** -- see the dedicated entry below. **Severity reasoning for the two categories the task flagged as non-obvious:** rules 14 and 15 both rely on "no trigger anywhere ever sets/targets this" -- a heuristic over the *shape* of the trigger graph, not a lookup against a fixed id namespace like the broken-reference rules. A course could pass a variable's value in from a source this analyzer doesn't model (there is none today, but the check can't prove a negative about the future), or a block could be deliberately reserved for a not-yet-built feature. Marking these WARNING (not ERROR, despite "the gate can never be satisfied" being described as a real authoring mistake in the task's own rule 15 text) keeps the analyzer honest about the difference between "this is definitely broken" (rules 1-7) and "this is very likely broken" (rules 14-15) -- COURSE-ANALYZER.md section 3.1's confidence distinction, informally, even though the full confidence field itself is deferred per the Step 1 entry above.
2026-07-19 | **Phase 4.5c: no existing pre-publish checklist to consolidate -- this is genuinely new.** ARCHITECTURE.md section 8 documents a "Pre-publish checklist" (missing alt text / captions / transcripts, blockable if a WCAG-compliance-mode setting is enabled) as part of the editor's design, and REQUIREMENTS.md P1-11 and P1-23 both describe similar checklist behavior -- but neither was ever built. Confirmed by grep (`pre-publish|prepublish|checklist`) across `packages/editor/src` and `packages/server/src` before starting this phase: zero matches outside this session's own new code. This is the same "schema/architecture doc describes a field or flow ahead of its implementation" pattern already established repeatedly in this codebase (P1-37/38's objectives/concepts, P1-35's answer-level feedback, multiple Phase 3.5 schema hooks) -- ARCHITECTURE.md 8 was aspirational documentation, not a description of shipped behavior. There was also no publish action of any kind in the running app to gate: `grep -rniE "publish"` across `packages/editor/src`/`packages/server/src` matched nothing (Section 5 below covers what this phase built to have something to gate in the first place). Nothing was refactored or replaced; `packages/schema/analyzer/` and the Course Health panel are new subsystems built directly against ARCHITECTURE-AUDIT.md 4.6/COURSE-ANALYZER.md's design, not a migration of prior ad hoc logic.
2026-07-19 | **Phase 4.5c: video caption checking (rule 10) skipped -- no field exists yet, flagged for Phase 5.** `content.asset_id`/`autoplay`/`loop` is the entire video/audio block content shape today (`packages/editor/src/lib/blockDefaults.js`'s `defaultContent()`, matching ARCHITECTURE.md 3.8's own note: "Minimal media block content (Phase 4 Part 3) -- no captions/transcript/timeline fields, those are Phase 5"). REQUIREMENTS.md P1-23 describes the full captions/transcript feature (Whisper auto-generation, caption editor, WebVTT/SRT override, a WCAG-compliance-mode setting that blocks publish on missing captions) as unbuilt Phase 5 work. Writing a rule against a `captions` field that doesn't exist would mean either a permanent no-op (dead code masquerading as coverage) or inventing a field ahead of the schema/UI that's supposed to own it -- exactly the kind of premature field this project's own "schema hooks added ahead of their UI" pattern is normally deliberate about, not accidental. **For whoever builds Phase 5 P1-23:** once video/audio blocks gain a real caption/transcript field (and P1-23's own WCAG-compliance-mode setting), add an `a11y.video_captions_missing` rule to `packages/schema/analyzer/rules.js` following the same shape as `ruleImageMissingAlt`, and add it to the `RULES` array in the same file -- no analyzer-engine changes needed, since `analyzeCourse()` already runs every registered rule uniformly.
2026-07-19 | **Phase 4.5c: minimal publish action and the unreachable-block reachability simplification.** No dedicated "publish" flow exists in this codebase (ARCHITECTURE.md 15's `course_versions`/push-all/lock-existing machinery is schema-and-table-only, unused by any route -- confirmed by reading `packages/server/src/routes/courses.js` in full); the one thing that already existed and was already generically accepted by `PATCH /courses/:id` is `courses.status`, defaulting to `'draft'`. Step 5's gating is built as the minimal "mark a course published" the task's own wording explicitly allows: a client-side `handlePublish()` in `CourseEditor.jsx` that force-saves, re-runs `analyzeCourse()` against the freshly-saved document (not the possibly-stale `findings` already computed for the current render), blocks with a toast and a jump to the Course Health tab if any ERROR finding exists, and otherwise PATCHes `status: 'published'` and shows a toast naming the surviving warning count (or a plain "Published." when there are none). This is deliberately not a build-out of dynamic-SCORM version control (push-all vs. lock-existing publish modes, `course_versions` snapshots, rollback) -- that remains Phase 6 work; today `status: 'published'` is the entire observable effect of publishing. Separately, rule 14's `visibility.initial: 'hidden'` branch (blocks hidden by the toggle rather than a `visibility_condition`) uses a deliberately coarse reachability check: "does *any* trigger target this block at all" (`trigger_targets_block`), not "does a SHOW_BLOCK/ENABLE_BLOCK specifically target it" -- because `dependency-index.js`'s own edge-building collapses SHOW_BLOCK/HIDE_BLOCK/ENABLE_BLOCK/DISABLE_BLOCK into one `trigger_targets_block` referenceType (see the 4.5b dependency-index-design entry), and re-deriving action-type-specific edges here would mean re-walking triggers, the exact "don't duplicate reference-tracking logic that already exists in the dependency index" this phase's own task explicitly ruled out. The result is a rule that catches the unambiguous case (nothing ever touches this block, it is certainly unreachable) but will not catch a block that some trigger only ever *hides further* -- documented here rather than silently shipped as if it were exhaustive.
2026-07-19 | **Phase 4.5c Step 6: regression test, 6/6 pass.** Built a dedicated test course (`Analyzer Test 4.5c`, via `POST /api/courses`) with one deliberately broken trigger reference, one image asset with empty `alt`, and one variable never referenced by any trigger. (1) **Broken reference blocks publish:** Course Health showed `Errors (1)`: `Text (1) references a variable ("ghostVar") that no longer exists.`; the top-bar badge read "⚠ 1 error"; clicking Publish force-saved, re-ran the analyzer, showed the toast "Cannot publish: 1 error must be fixed first.", jumped to the Course Health tab, and left `courses.status` at `'draft'` (confirmed via a direct API read) -- PASS. (2) **Missing alt text is a warning, not a blocker:** listed under `Warnings`, non-blocking -- PASS (see combined test below for "doesn't block publish" proof). (3) **Unused variable flagged:** `Variable "orphanVar" is defined but never used in any trigger or visibility condition.` under Warnings -- PASS. (4) **Click-to-navigate:** clicking the alt-text finding switched the right panel to that block's own "Image Settings" (confirmed via panel heading and the block's `block-wrapper--selected` class in the DOM), and correctly called `scrollIntoView({behavior:'smooth', block:'center'})` on the exact right `data-block-id` element (verified by intercepting `Element.prototype.scrollIntoView` before the click) -- PASS. The scroll position itself did not visibly move in the automated browser tool used for this test (`smooth` scroll animation does not appear to progress under CDP automation control in this environment), but the call itself -- correct element, correct args -- is the part that matters for real users, so this is recorded as an environment limitation of the testing tool, not a product bug. (5) **Fix all findings -> clean publish:** removed the broken trigger, added real alt text, and deleted the unused variable (via direct data edits, since the broken trigger lived on a `text` block, which the registry correctly gives no Triggers UI at all -- there was no in-app path to "edit" a trigger that shouldn't exist on that block type in the first place, an artifact of how the test course was seeded, not a product gap) -- Course Health showed "✓ No issues found. This course is ready to publish.", the top-bar badge disappeared entirely, and Publish produced the toast "Published." with `courses.status` confirmed `'published'` via API -- PASS. (6) **Works against pre-4.5c courses:** opened "Regression Test 4.5b" (built during the 4.5b session, all 14 block types) -- zero findings, zero console errors; opened "Phase 3 TEST Course" (a real, heavily-edited pre-4.5a course migrated live on load) -- 18 warnings, all `a11y.image_alt_missing`/similar, zero console errors, zero crashes, confirming the analyzer runs correctly against a document that passed through the full 4.5a migration and 4.5b registry/dependency-index stack before reaching it. All 6: **PASS**. One real UI bug found and fixed during this pass, unrelated to the analyzer's own logic: the Course Health tab's label didn't fit alongside Course/Page/Player/Variables in the settings panel's fixed-width tab bar and visually clipped -- fixed by adding `flex-wrap: wrap` to `.settings-panel__tabs` (and `white-space: nowrap` per tab) so a 5th+ tab drops to its own row instead of overflowing.

2026-07-19 | **Phase 4.6 Step 1: `settingsGroups` is derived from existing registry fields, not a hand-maintained parallel list.** The task asked for a `settingsGroups`-or-equivalent property driving Basic/Advanced per block type "data-driven, not a one-off conditional." Rather than manually authoring a `{basic: [...], advanced: [...]}` list per block type (14 more places for a field to drift out of sync with reality), `packages/schema/block-registry.js` computes it once from fields the registry already carries: `basic: hasSettings ? ['content'] : []` (a block either has real content-editing fields or it doesn't -- already tracked) and `advanced: [...UNIVERSAL_ADVANCED, 'triggers']` when `validEvents.length > 0`, else just `UNIVERSAL_ADVANCED` (`['blockName', 'visibility', 'facultyNotes']`, present on every block type regardless of its own content shape). This runs once over `BLOCK_REGISTRY` at module load (`for (const def of Object.values(BLOCK_REGISTRY)) def.settingsGroups = settingsGroupsFor(def)`), so a new block type added to the registry with its own `hasSettings`/`validEvents` automatically gets correct Basic/Advanced grouping with zero settingsGroups-specific code to write. `SettingsPanel.jsx` reads `definition.settingsGroups` and branches purely on membership (`groups.basic.includes('content')`, `groups.advanced.includes('triggers')` etc.) -- it has no per-block-type knowledge of its own. | Why derive instead of hand-author: the registry's whole reason for existing (4.5b) was ending block-type knowledge scatter across the codebase; inventing a second hand-maintained classification here would have reintroduced exactly the problem 4.5b just fixed, and every one of the 14 existing block types already expressed everything needed to derive the grouping correctly.
2026-07-19 | **Phase 4.6 Step 2/5: view-state (panel collapse, Focus Mode, group collapse) is deliberately excluded from the undo/redo stack.** `leftPanelCollapsed`/`rightPanelCollapsed`/`focusMode` (`CourseEditor.jsx`) and `collapsedGroups` (`PageList.jsx`) are plain `useState`, never routed through `updateCourseJson` (the single choke point ARCHITECTURE.md 3.9 designates for all document mutations and their undo snapshots). Confirmed live during Step 10's regression pass: toggling both panels and Focus Mode repeatedly left the Undo button in whatever enabled/disabled state the last real *document* edit had left it in, and pressing Undo after a Focus Mode toggle undid the last content change (a block insertion) while Focus Mode itself stayed active and untouched -- proving the two state systems are fully decoupled, not just conventionally separated. | Why: panel layout and which modules happen to be expanded are exactly "how you're currently looking at the document," not "what the document is" -- the same distinction the task's own wording drew ("view state, not document state"). Routing them through the undo stack would mean pressing Cmd+Z to fix a typo could instead silently re-expand a panel the author had just collapsed, an unrelated and confusing side effect; keeping them as ordinary component state means they reset to sensible defaults on reload (session-level, not persisted) with no risk of ever colliding with a real content edit's undo entry.
2026-07-19 | **Phase 4.6 Step 9: bulk alt-text review saves incrementally per keystroke, matching the pre-existing single-image alt-text field exactly -- no new save mechanism invented.** `ImageBlockSettings` (`packages/editor/src/components/blocks/ImageBlock.jsx`, pre-dating this phase) already edits `asset.alt` via `onChange={(e) => onUpdateCourseAsset(asset.asset_id, { alt: e.target.value })}` on every keystroke, relying on `updateCourseJson`'s built-in typing-burst debounce (`TYPING_BURST_MS`) to coalesce a run of keystrokes into one undo entry rather than one-entry-per-character. `BulkAltTextReview.jsx` (new, Step 9) calls the identical `onUpdateCourseAsset(item.asset_id, { alt: e.target.value })` per row -- same handler, same prop, same debounce grouping -- so every field in the bulk screen is automatically undo/redo-capable via the exact mechanism already proven correct for the single-field case, with zero new autosave code. A "Save all" button was considered and rejected: the task allowed either pattern ("use judgment, whichever fits the existing autosave pattern"), and inventing a batch-save path would have meant either a second, differently-shaped autosave call the rest of the editor doesn't have, or manually looping N `onUpdateCourseAsset` calls through the same debounce and getting incremental save's behavior anyway with extra steps. One deliberate UX choice beyond the save pattern: the screen's asset list is captured once via a `useState` initializer, not re-filtered live from the `assets` prop on every render -- if it were re-filtered live, a row would vanish from the list the instant its alt text became non-empty, yanking keyboard focus out from under the author mid-word. Each row instead looks up its own current value from the live `assets` prop (so the input always reflects real, saved state and a page reload mid-review loses nothing) while staying visually present with a checkmark once done, until the whole screen is closed. | Why match the existing pattern exactly: consistency with `ImageBlockSettings`'s already-shipped, already-tested alt-text editing means an author sees identical save behavior whether they're fixing one image from its own block settings or six from the bulk screen, and the undo/redo guarantee didn't need re-proving from scratch.
2026-07-19 | **Phase 4.6 Step 7: no existing visible label was renamed -- confirmed by construction, not just by intent.** Every one of the 7 `InfoTooltip` call sites (Navigation mode, Completion rule, Visibility, Continue gate, Variables, Faculty notes, Triggers) was added as a sibling element next to the pre-existing label text, never by editing the label string itself -- verified by diffing each touched file's label text against its pre-4.6 value (all identical) and by browser-testing that every existing label reads exactly as it did before this phase. `InfoTooltip.jsx` is a small standalone component (real `<button>` trigger with `aria-label="More information"`, `aria-describedby` pointing at a `role="tooltip"` bubble, `onFocus`/`onMouseEnter` show and `onBlur`/`onMouseLeave`/Escape hide) so adding it next to a label is always a pure addition, structurally incapable of altering the label's own text. Two real bugs were found and fixed while wiring these up, both introduced earlier in this same phase (Steps 4-7), neither pre-existing: (1) `.info-tooltip__bubble` was positioned `left: 0` (extending rightward from its trigger), but every trigger lives inside the far-right settings panel -- for a trigger already near the panel's right edge, the 240px-wide bubble ran off the right edge of the viewport entirely (confirmed via `getBoundingClientRect()`: bubble `right: 1488` against a 1280px-wide window), making the tooltip invisible/unreachable rather than merely misaligned; fixed by switching to `right: 0` so bubbles extend leftward into the panel, which is correct for every call site since all of them sit in the same right-hand panel. (2) The bubble's `background: var(--color-text)` referenced a CSS custom property that doesn't exist anywhere in the brand token set (the real token is `--color-text-primary`; grep confirmed `--color-text` -- as opposed to `--color-text-primary`/`--color-text-secondary`/`--color-text-muted` -- has zero definitions), so the background silently resolved to transparent, making the bubble's white text unreadable against the light settings panel; the same invalid token, introduced by this same phase, had also leaked into two other Step 4.6 additions (`.page-list__group-title`, Step 5; `.block-wrapper--selected .block-wrapper__label`, Step 6) -- all three fixed to `--color-text-primary` in the same pass. | Why record this as a construction-time guarantee rather than a testing checklist item: the task explicitly called out this constraint ("Do NOT rename any existing visible label") as a risk to avoid, and the additive-sibling pattern makes violating it require a deliberate separate edit to the label string, not an accidental side effect of adding a tooltip -- the two bugs that were found are real regressions from this phase's own work, not near-misses on the renaming rule.
2026-07-19 | **Phase 4.6 Step 10: regression test, 11/11 pass.** Ran the task's own 11-point checklist against a mix of pre-existing courses and two purpose-built test courses (both deleted via `DELETE /api/courses/:id` after use, matching this codebase's established pattern of not leaving throwaway test data in the shared dev library). (1) Pre-4.6 course opened cleanly, zero console errors -- PASS. (2) Text/Heading/Knowledge Check block settings panels all show Basic content by default with Advanced collapsed -- PASS. (3) Both side panels and Focus Mode collapse/expand, canvas visibly resizes each time -- PASS. (4) A between-block "+" opened the picker at the exact clicked position; the inserted block's `contenteditable` field received focus, confirmed via `document.activeElement` -- PASS. (5) Preview opens with Phone/Tablet/Desktop controls inside the preview view itself (not the main toolbar); More Tools menu contains Media Library (opened successfully) and Save as Template -- PASS. (6) A 2-module/2-page-each grouped structure (seeded via direct `PATCH .../course_json` to avoid dozens of UI clicks) rendered with clear bold-uppercase module headers, indented pages, and an "Ungrouped pages" section matching UX-AUDIT.md's own example layout; a module and a page both collapsed/expanded and inline-renamed correctly -- PASS. (7) Selected blocks show the strong blue outline with a visible type/label; unselected blocks show lighter borders with hover-reveal toolbar icons -- PASS. (8) Tooltips on Navigation mode, Completion rule, Visibility, Continue gate, Variables, Faculty notes, and Triggers all reveal on hover and on keyboard focus, dismiss on Escape without trapping focus, and Tab correctly advances to the next real control afterward -- PASS. (9) A 6-image test course grouped into one "6 images missing alt text" Course Health warning; "Review all" opened the bulk screen with empty (not caption-prefilled) alt-text inputs; filling two in updated the live count from 6 to 4 in both the toolbar badge and the Course Health panel simultaneously, and closing the screen left Course Health re-run and showing the reduced count -- PASS. (10) Undo correctly reverted a real document edit (a block insertion) while Focus Mode, entered afterward, remained untouched by the Undo click -- PASS, see the dedicated Step 2/5 entry above. (11) Every interaction above was independently confirmed keyboard-reachable: insertion points, panel-collapse buttons, and tooltip triggers are all real `<button>` elements in the natural tab order (not click-only `div`s), and the bulk alt-text screen's inputs are real `<input>` elements reachable the same way. One re-tested-but-not-a-bug finding: reproducing PageList's inline rename via `double_click` followed by a raw `Cmd+A` keypress in the automated browser tool produced visibly concatenated text (`"PracticeHands-on Practice"`) -- but re-doing the identical rename using `element.select()` (a real native full-selection call) before typing produced the correct replaced text every time, for both a page and a module title, conclusively confirming (a second time, after Phase 4.6's own earlier live investigation during Step 5) that this is an artifact of the test tool's synthetic `Cmd+A`/double-click not reliably reproducing native browser text selection, not a defect in `InlineRenameField`'s controlled-input commit logic. All 11: **PASS**. Real bugs found and fixed during this pass are recorded in the Step 7 entry above (tooltip overflow + invalid CSS token), not repeated here.

2026-07-20 | **Post-4.6 QA Bug 1: YouTube embed "refused to connect" was a URL-format problem, not a sandbox problem -- confirmed by isolating the two variables live, not by reasoning from documentation alone.** Reproduced with a real `https://www.youtube.com/watch?v=<id>` URL in the embed block: blank iframe, no visible content, with the sandbox value completely unchanged from before this fix. Swapping ONLY the URL to `https://www.youtube.com/embed/<id>` -- same unmodified sandbox (`allow-scripts allow-same-origin allow-popups`) -- rendered the full YouTube player (title, channel avatar, thumbnail, Watch-on-YouTube overlay) immediately. This isolates the cause: YouTube's normal watch page and youtu.be short links send an X-Frame-Options/CSP `frame-ancestors` response that refuses to be framed by any other origin (the browser's "refused to connect" wording is exactly Chrome's standard phrasing for that class of failure); only the dedicated `/embed/<id>` endpoint is designed to be embedded. No sandbox permission can work around a same-origin framing refusal -- sandbox controls what already-loaded content is allowed to do, not whether a cross-origin response permits being framed in the first place. **Fix, two parts:** (1) `packages/editor/src/lib/embedUrl.js` (new) exports `toEmbeddableUrl(url)`, a pure regex-based normalizer recognizing `youtube.com/watch?v=`, `m.youtube.com/watch?v=`, and `youtu.be/<id>` (with or without trailing query params like `&t=30s`) and rewriting each to `https://www.youtube.com/embed/<id>`; already-`/embed/` URLs and non-YouTube domains pass through completely unchanged (unit-verified against 12 cases including empty string). Vimeo gets the identical treatment (`vimeo.com/<id>` -> `player.vimeo.com/video/<id>`) since it has the exact same watch-page-vs-player-domain split and is already one of the block's documented allowed domains -- not scope creep, the same bug class hit twice. Wired into `EmbedBlockEditor` (`packages/editor/src/components/blocks/EmbedBlock.jsx`) via `onPaste` (converts immediately, matching how an author actually gets a URL onto their clipboard -- verified with a dispatched `ClipboardEvent` carrying a real `DataTransfer`) and `onBlur` (a safety net for a manually-typed, not pasted, URL -- verified live by typing a raw watch URL and tabbing away). Neither handler touches partial/in-progress typing: the regexes only match a complete, well-formed URL, so intermediate keystroke states never get prematurely rewritten. (2) Separately, `DEFAULT_EMBED_SANDBOX` (`packages/editor/src/lib/blockDefaults.js`) was widened from `allow-scripts allow-same-origin allow-popups` to `allow-scripts allow-same-origin allow-presentation allow-popups`, mirrored in the player's own fallback default (`packages/player/src/blocks/EmbedBlock.jsx`, a separate literal since editor and player are independently deployed packages that can't share a source import) -- `allow-presentation` is what YouTube/Vimeo's own fullscreen and picture-in-picture/casting controls need once the player has actually loaded; it did not affect whether THIS specific failure reproduced (confirmed: the /embed/ URL rendered correctly even before this token was added), so it's forward-looking completeness matching the task's own stated minimum, not itself the fix. `allow-popups-to-escape-sandbox` remains excluded everywhere, unchanged: `EmbedBlock.jsx` in both `packages/editor` and `packages/player` still strip that specific token from any sandbox string before applying it (defensive, in case it were ever present in saved content) -- verified by reading both files' existing strip logic before touching either, and neither was modified. Only blocks created AFTER this change pick up the wider default; existing blocks' saved `content.sandbox` values are left exactly as authored (no forced migration), confirmed via a direct API read after adding a new embed block alongside three pre-existing ones. **Regression check:** the pre-existing DigitalScope-style embed (both a synthetic `example.com` stand-in and, separately, a real course's existing `digitalscope.org` WSI viewer, which loaded and rendered its actual slide image live in this same test pass) continued to load exactly as before -- neither the URL normalizer (non-YouTube/Vimeo domains are a no-op) nor the wider sandbox default (additive, not restrictive) touch that path. | Why the sandbox is not the whole story here: a sandbox attribute governs the CONTENT's capabilities once it's already been allowed to load into the frame -- it has no mechanism to override the destination server's own decision (via X-Frame-Options/CSP) about whether it will be framed at all. Chasing this as a sandbox-permissions problem alone would never have fixed it, which is why this entry leads with confirming the URL was the actual cause via a controlled live comparison rather than starting from "widen the sandbox and see."
2026-07-20 | **Post-4.6 QA Bug 2: InfoTooltip positioning is now clamp-based (measured and shifted to fit), not a second one-directional patch.** The 4.6 fix anchored the bubble `right: 0` (always extending leftward) to solve triggers overflowing the viewport's right edge. QA correctly identified this as directional, not positional: the "Variables" tooltip (a trigger sitting toward the LEFT portion of the ~280px-wide settings panel) then overflowed the other way. A first attempt at a binary left/right flip (pick whichever side has more room, based on `window.innerWidth`) was tried, verified live, and found to still be insufficient: the settings panel is narrower (~280px) than the bubble (240px), so depending on exactly where a given trigger sits, NEITHER a pure flush-left NOR flush-right placement fully avoids overflowing something -- confirmed live via `getBoundingClientRect()` (the "Variables" bubble's LEFT edge sat 132px past the panel's own left edge even under the corrected binary-flip logic, and the resulting rendered text was visibly truncated: "ourse" instead of "course", etc.). Root cause of the truncation specifically: `.settings-panel` sets `overflow-y: auto` without an explicit `overflow-x`, and per the CSS Overflow spec (confirmed live via `getComputedStyle`, which reported `overflowX: "auto"`), a UA must compute the unset axis as `auto` too when the other axis isn't `visible` -- so the panel silently clips/scrolls horizontally as well, meaning a bubble that fits the *browser viewport* can still be clipped by the *panel's own scroll box* well before reaching the true screen edge. **Final fix** (`packages/editor/src/components/InfoTooltip.jsx`): on open, a `useLayoutEffect` measures the trigger's and bubble's real `getBoundingClientRect()`, walks up from the trigger to find the nearest ancestor whose computed `overflow-x`/`overflow-y` is `auto` or `scroll` (falling back to the full viewport if none exists), and computes a `clamp(idealStart, size, boundsStart, boundsEnd)` position: the bubble prefers sitting flush with the trigger's own edge (its natural, `left:0`-equivalent spot) but gets shifted back only as far as necessary to keep `[start, start+size]` fully inside those bounds with a 16px margin -- the same "flip, then shift to fit" idea a full positioning library implements, applied via two numbers (`offset.left`/`offset.top` as inline style) rather than a library. Runs in `useLayoutEffect`, not `useEffect`, so the corrected position is already in place at first paint -- no flash of the wrong placement, and no resize/scroll listeners needed for a tooltip that's only open a few seconds. Checked the project's dependencies before writing any of this: no Popper.js, Floating UI, Tippy, or similar positioning library is present anywhere in this repo, so a small measurement was the right size of solution, not a new dependency for one tooltip component. Vertical placement (`above`/`below`) uses the identical clamp logic against the same bounds, in case a tooltip ever opens near the top/bottom of a shorter viewport, though this wasn't independently reported as broken. **Verified at both edges in the same session:** "Variables" (left-ish trigger, narrow panel) now renders fully inside the panel with complete, untruncated text, confirmed via both `getBoundingClientRect()` (bubble bounds fully within panel bounds minus margin) and a screenshot; "Navigation mode" and "Completion rule" (the original right-edge cases from the 4.6 fix) re-tested after this rewrite and still render correctly with no regression, same clamp math naturally handling both. Escape-to-dismiss and focus-return (no focus trap) re-verified unaffected, since none of `InfoTooltip`'s event handlers changed -- only the position-calculation internals did. | Why clamp instead of a second flip: a strict binary flip only ever asks "which single side has more room," which breaks down exactly when NEITHER side has enough room on its own (a container narrower than the bubble) -- exactly this codebase's real layout. Clamping asks "how close to its natural position can the bubble get while staying fully visible," which degrades gracefully in that same narrow-container case instead of just picking the less-bad of two still-broken options.

## Pending: Ethos UAT test
- Upload mnemonify-sample-launcher.zip to Ethos UAT when site is restored
- Confirm course loads, completes, and reports score correctly
- Document any Ethos-specific quirks in DECISIONS.md
