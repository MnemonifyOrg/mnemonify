{
  "name": "mnemonify",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=packages/server",
    "package:scorm": "node packages/launcher/build.js"
  }
}
